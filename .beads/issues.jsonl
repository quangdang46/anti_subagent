{"id":"anti_subagent-0kl","title":"Test: anti report channel unit + integration tests","description":"## Context\nPhase 1.0 of PLAN-v3-runtime-engine. Comprehensive tests for the Peer→anti report channel. These tests must be thorough enough to catch any regression in the report flow.\n\n## Unit tests (in report.rs #[cfg(test)])\n\n### Happy path\n1. **completed + valid commit → Submitted**: Create work_item with peer_id, create temp git repo with a commit, call handle_report(status=completed, commit=sha) → work state transitions to Submitted, verification runs, evidence persisted\n\n### Validation failures\n2. **Task not found**: Call handle_report(task_id=\"nonexistent\") → TaskNotFound error\n3. **Task not assigned (empty peer_id)**: Create work_item with peer_id=\"\", call handle_report → TaskNotAssigned error\n4. **Invalid status string**: Call handle_report(status=\"invalid\") → InvalidStatus error\n5. **Empty task_id**: Call handle_report(task_id=\"\") → InvalidTaskId error\n\n### Commit verification\n6. **Commit doesn't exist**: Call handle_report(status=completed, commit=\"deadbeef\") in a repo that doesn't have that commit → CommitNotFound error\n7. **Commit exists but in wrong workspace**: Verify that workspace_path is resolved from agent record, not from peer input\n\n### Verification integration\n8. **Verify passes → Submitted**: complete report with passing verify → state = Submitted\n9. **Verify fails → NeedsRevision**: complete report with failing verify → state = NeedsRevision\n\n### Status-specific behavior\n10. **Failed status → NeedsRevision**: report with status=failed → state = NeedsRevision (revision < max_revisions)\n11. **Failed status → Failed (exhausted)**: report on work_item with revision >= max_revisions → state = Failed\n12. **Progress status → no state change**: report with status=progress → work state unchanged, event emitted\n13. **Question status → no state change**: report with status=question → work state unchanged, event emitted\n\n### Edge cases\n14. **Concurrent reports on same task**: Two handle_report calls on same task_id → first succeeds, second gets optimistic-lock error\n15. **Report after task already accepted**: Report on Accepted work item → appropriate error or no-op\n16. **Report with commit when status != Completed**: commit field ignored (not error)\n\n### Test infrastructure\n- Create temp git repos with `git init` + `git commit --allow-empty` for commit verification\n- Use temp dirs for SQLite state (existing pattern in store.rs tests)\n- Each test cleans up after itself\n\n## Integration tests (in anti-integration/tests/)\n17. **E2E report flow**: Start daemon → spawn peer → peer runs `anti report` via subprocess → daemon processes → work state updated → verify via `anti work list`\n\n## Files\n- MODIFY: `crates/anti-daemon/src/report.rs` (unit tests in #[cfg(test)] mod tests)\n- MODIFY: `crates/anti-integration/tests/` (new integration test file)","status":"closed","priority":1,"issue_type":"test","created_at":"2026-08-17T15:01:41.418654Z","created_by":"tranquangdang21","updated_at":"2026-08-17T15:46:06.281540Z","closed_at":"2026-08-17T15:46:06.281464Z","close_reason":"Implemented: 11 unit tests (7 existing + 4 new with git repo setup). Added Submitted→Failed/NeedsRevision→Failed transitions for exhausted case. 62/62 tests pass. Pushed as 0831b53.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-0kl","depends_on_id":"anti_subagent-8yz","type":"blocks","created_at":"2026-08-17T15:09:34.689113Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-0kl","depends_on_id":"anti_subagent-yib","type":"blocks","created_at":"2026-08-17T15:09:34.619175Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-0oq","title":"1.1: Staged task state machine","description":"STAGED TASK STATE MACHINE (P1): Current WorkItemState has 7 states (Pending/InProgress/Submitted/Verified/Accepted/NeedsRevision/Rejected). Need to expand to support full lifecycle.\n\nNEW STATES (12 total):\n- RECEIVED: task assigned to peer\n- EXPLORED: peer has investigated codebase\n- PLANNED: implementation plan created\n- EXECUTING: peer actively coding\n- EXECUTED: code written, awaiting verification\n- VERIFYING: verification in progress\n- VERIFIED: verification passed (prerequisite for accept)\n- ACCEPTED: lead accepted (terminal)\n- FAILED: execution failed (terminal)\n- REJECTED: verification failed, needs fix\n- FIXING: peer fixing issues\n- EXHAUSTED: max_revisions exceeded (terminal)\n\nTRANSITION TABLE (can_transition):\n- Pending → InProgress (existing)\n- InProgress → Submitted (existing)\n- Submitted → Verified (via verify command)\n- Submitted → NeedsRevision (via reject)\n- Verified → Accepted (via accept)\n- Verified → NeedsRevision (via reject)\n- NeedsRevision → InProgress (via re-submit)\n- NEED NEW:\n  - Any → Received (task assignment)\n  - Received → Explored (investigation done)\n  - Explored → Planned (plan created)\n  - Planned → Executing (implementation start)\n  - Executing → Executed (code complete)\n  - Executing → Failed (error)\n  - Executed → Verifying (verify started)\n  - Verifying → Verified (pass)\n  - Verifying → Rejected (fail)\n  - Rejected → Fixing (fix started)\n  - Fixing → Executing (re-implement)\n  - Rejected → Exhausted (max_revisions)\n\nBACKWARD COMPATIBILITY:\n- Existing Pending/InProgress/Submitted/Verified/Accepted/NeedsRevision/Rejected states kept\n- New states are ADDITIONS, not replacements\n- Old CLI commands still work (submit → Submitted, reject → NeedsRevision)\n- New staged workflow is opt-in via new IPC commands\n\nFiles to modify:\n- crates/anti-core/src/work.rs — expand WorkItemState enum + can_transition\n- crates/anti-daemon/src/store.rs — migration for new state columns\n\nTests:\n- staged pipeline: Received→Explored→Planned→Executing→Executed→Verifying→Verified→Accepted\n- fix loop: Verifying→Rejected→Fixing→Executing→Verifying→Verified→Accepted\n- exhaustion: Rejected (rev 3) → Exhausted (terminal)\n- backward compat: old Pending→InProgress→Submitted still works","status":"done","priority":2,"issue_type":"feature","created_at":"2026-08-17T06:36:04.260431600Z","created_by":"unknown","updated_at":"2026-08-17T06:59:15.376705400Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none"}
{"id":"anti_subagent-1se","title":"1.3: Disposition contracts (basic)","description":"DISPOSITION CONTRACTS (P1): Current model.rs has Disposition enum with 6 variants (Engineer/Architect/Reviewer/Scout/ProofAuditor/Shadow) but no behavioral contracts.\n\nTARGET: DispositionContract struct defines what each disposition CAN and CANNOT do:\n- name: Disposition (existing enum)\n- allowed_tools: Vec<String> (tools this disposition may use)\n- denied_tools: Vec<String> (tools this disposition may NOT use)\n- can_approve_own_work: bool (Engineer=false, ProofAuditor=false)\n- requires_evidence: bool (ProofAuditor=true)\n- max_concurrent: usize (how many peers with this disposition can run)\n\nCONTRACT TABLE:\n| Disposition | CAN | CANNOT | Self-Approve | Evidence Required |\n|---|---|---|---|---|\n| Engineer | read, write, test, edit | approve own work | No | No |\n| Architect | read, design, plan | modify implementation | No | No |\n| Scout | read, search, inspect | modify source | No | No |\n| Reviewer | inspect, test, challenge | silently modify | No | Yes |\n| ProofAuditor | read, test, inspect git/logs | modify source | No | Yes |\n| Shadow | read, observe | modify anything | No | No |\n\nFILES:\n- crates/anti-core/src/disposition.rs (NEW) — DispositionContract + contracts for each disposition\n- crates/anti-core/src/model.rs — keep existing Disposition enum, reference from contracts\n\nINTEGRATION:\n- PeerManager checks DispositionContract before allowing tool execution\n- Guard (ipc.rs GuardCheck) enhanced to use contracts\n- Verify handler requires ProofAuditor disposition\n\nTests:\n- Engineer denied self-approval → error\n- Scout denied write tool → error\n- ProofAuditor requires evidence → error if no EvidenceRecord\n- Each disposition's allowed/denied tool lists correct","status":"done","priority":2,"issue_type":"feature","created_at":"2026-08-17T06:36:04.825991300Z","created_by":"unknown","updated_at":"2026-08-17T07:03:49.737669300Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none"}
{"id":"anti_subagent-2bw","title":"Domain: Closed enums ReviewVerdict and VerificationStatus","description":"Add ReviewVerdict enum (Accept, Reject, Escalate) serde SCREAMING_SNAKE_CASE. Add VerificationStatus enum (Open, EvidenceReady, Verifying, Verified, Failed, Uncertain) serde SCREAMING_SNAKE_CASE. Add to crates/anti-core/src/work.rs alongside WorkItem. ReviewVerdict::Escalate = watchdog action for silent lead. VerificationStatus drives evidence-gating (Open→EvidenceReady→Verifying→Verified). Tests: serde roundtrip serialization, exhaustive match compile-time guarantee.","status":"closed","priority":1,"issue_type":"feature","estimated_minutes":15,"created_at":"2026-08-16T17:08:04.287139Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:03:49.505772Z","closed_at":"2026-08-17T16:03:49.505718Z","close_reason":"Already implemented in work.rs: ReviewVerdict (Accept/Reject/Escalate), VerificationStatus (Open/EvidenceReady/Verifying/Verified/Failed/Uncertain), VerifyProfile, VerifyStatus, VerificationResult. With exhaustive match test. No changes needed.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","labels":["anti-core","phase-0"]}
{"id":"anti_subagent-2n0","title":"Replace subprocess calls with library API","description":"## Background\n\nanti-workspace currently shells out to the `treehouse` binary via `Command::new(\"treehouse\")`. This adds ~50ms overhead per call and requires the binary to be installed. Replace with direct library calls to AntiPool.\n\n## Goal\n\nReplace all `Command::new(\"treehouse\")` calls in anti-workspace with AntiPool method calls.\n\n## Scope\n\n### Modified: `crates/anti-workspace/src/lib.rs`\n\n#### Before (subprocess)\n\n```rust\npub fn acquire(repo_root: &Path, holder: &str) -> Result<Lease> {\n    let output = Command::new(\"treehouse\")\n        .args([\"get\", \"--lease\", \"--lease-holder\", holder, \"--json\"])\n        .current_dir(repo_root)\n        .output()?;\n    // ... parse JSON\n}\n```\n\n#### After (library call)\n\n```rust\npub fn acquire(pool: &AntiPool, repo_root: &Path, remote_url: Option<&str>, holder: &str) -> Result<Lease> {\n    pool.acquire(repo_root, remote_url, holder, None)\n        .map_err(|e| WorkspaceError::Treehouse(e.to_string()))\n}\n\npub fn acquire_with_ttl(pool: &AntiPool, repo_root: &Path, remote_url: Option<&str>, holder: &str, ttl: Duration) -> Result<Lease> {\n    pool.acquire(repo_root, remote_url, holder, Some(ttl))\n        .map_err(|e| WorkspaceError::Treehouse(e.to_string()))\n}\n\npub fn release(pool: &AntiPool, repo_root: &Path, remote_url: Option<&str>, lease: &Lease) -> Result<()> {\n    pool.release(repo_root, remote_url, &lease.path)\n        .map_err(|e| WorkspaceError::Treehouse(e.to_string()))\n}\n```\n\n### Functions to replace\n\n1. `acquire()` → `pool.acquire()`\n2. `release_if_lease()` → `pool.release()`\n3. `verify_lease()` → keep (verification logic stays)\n4. `status()` → `pool.status()` (if needed)\n\n### CAS module (cas.rs)\n\nKeep unchanged — CAS write protection is independent of treehouse.\n\n## Considerations\n\n- Backward compatibility: keep old functions as deprecated wrappers during transition\n- Error type mapping: `PoolError` → `WorkspaceError`\n- The `Lease` struct stays the same (path, lease_id, holder)\n\n## Verification\n\n- Existing integration tests pass (T1-T6)\n- No subprocess calls in `strace` output\n- Acquire/release latency < 5ms (vs ~50ms before)\n- `cargo test -p anti-workspace` passes","status":"closed","priority":3,"issue_type":"feature","created_at":"2026-08-17T16:24:22.669483Z","created_by":"tranquangdang21","updated_at":"2026-08-19T04:32:26.525059Z","closed_at":"2026-08-19T04:32:26.524649400Z","close_reason":"Replaced subprocess calls with library API. Treehouse struct now delegates to AntiPool. anti-daemon uses make_treehouse(state_dir) with Arc for 'static closures. 15 workspace + 38 daemon tests pass. All formatted, clippy clean.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-2n0","depends_on_id":"anti_subagent-hsi","type":"blocks","created_at":"2026-08-17T16:26:10.078206Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-2zl","title":"Phase 3: Scheduling & Routing","description":"P3 — 2 weeks. Config-based model routing (disposition × complexity → capability tier → provider config), parallel scheduler (resource-aware, dependency-aware). Depends on Phase 2 orchestration.","status":"done","priority":4,"issue_type":"feature","created_at":"2026-08-17T06:36:24.886475300Z","created_by":"unknown","updated_at":"2026-08-17T07:12:20.126661900Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none","dependencies":[{"issue_id":"anti_subagent-2zl","depends_on_id":"anti_subagent-d26","type":"blocks","created_at":"2026-08-17T06:36:32.728744200Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-35l","title":"Daemon: lead handoff — context handoff after compaction","description":"## Context\nPhase 5 of PLAN-v3-runtime-engine. When a Lead Claude session suffers context degradation (compaction), it needs to hand off to a new session without losing state.\n\nContext degradation happens when Claude's conversation grows too long and the system summarizes it. After compaction, the Lead may lose specific details about in-progress tasks, decisions made, and lessons learned. Handoff preserves this context.\n\n## What to build\nCreate or enhance `crates/anti-daemon/src/handoff.rs`:\n\n## HandoffContext struct\n```rust\npub struct HandoffContext {\n    pub previous_lead_id: String,\n    pub active_tasks: Vec<TaskSnapshot>,\n    pub completed_decisions: Vec<Decision>,\n    pub lessons: Vec<String>,\n    pub workspace_state: WorkspaceSnapshot,\n    pub timestamp: String,\n}\n\npub struct TaskSnapshot {\n    pub task_id: String,\n    pub peer_id: String,\n    pub state: WorkItemState,\n    pub description: String,\n    pub evidence: Option<String>,\n}\n\npub struct Decision {\n    pub timestamp: String,\n    pub context: String,\n    pub decision: String,\n    pub rationale: String,\n}\n```\n\n## Handoff flow\n```\nContextDegradation detected (via LifecycleBus event)\n  → Capture: active tasks, in-progress work, decisions made, lessons learned\n  → Serialize to HandoffContext\n  → Write HandoffContext to state dir (JSON file)\n  → Emit LeadHandoff event\n  → (Lead session reads HandoffContext as initial context)\n```\n\n## Key invariants\n- Handoff is explicit, not automatic. The Lead (or a watchdog) must detect degradation and trigger handoff.\n- HandoffContext is a snapshot, not a live bridge. The old Lead is archived, the new Lead starts fresh with the snapshot.\n- Handoff prevents infinite handoff loops: each handoff increments a generation counter. Max 3 handoffs per task before escalation.\n\n## Files\n- NEW: `crates/anti-daemon/src/handoff.rs`\n- MODIFY: `crates/anti-daemon/src/main.rs` (register handoff handler on LifecycleBus)","status":"closed","priority":4,"issue_type":"feature","created_at":"2026-08-17T15:09:23.464676Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:14:47.098537Z","closed_at":"2026-08-17T16:14:47.098241Z","close_reason":"Already implemented in handoff.rs: LeadHandoff struct, HandoffReason, HandoffContext, Lesson, LessonCategory, to_json/from_json. 2 tests pass. No changes needed.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-35l","depends_on_id":"anti_subagent-z7m","type":"blocks","created_at":"2026-08-17T15:09:46.138055Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-38i","title":"Phase 0: Safety & Correctness","description":"P0 priority — Fix critical safety bugs before any feature work. Includes: (1) Replace pkill -f with Treehouse.return() for workspace cleanup, (2) Add crash cleanup lifecycle with PeerCrashed event, (3) Add VerifyProfile-based verification before accept. All existing tests must pass. New safety tests required.","status":"done","priority":1,"issue_type":"feature","created_at":"2026-08-17T06:34:16.503895900Z","created_by":"unknown","updated_at":"2026-08-17T06:57:13.300311500Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none"}
{"id":"anti_subagent-3qp","title":"Domain: WorkItem state machine with evidence-gated lifecycle","description":"Create crates/anti-core/src/work.rs: WorkItemState enum (Pending, InProgress, Submitted, Verified, Accepted, NeedsRevision, Rejected) with serde SCREAMING_SNAKE_CASE. WorkItem struct { id, task_node_id, peer_id, lead_id, state, revision, max_revisions, evidence: Option<EvidenceRef>, review_verdict, submitted_at, review_deadline, created_at, updated_at }. EvidenceRef { sha256, artifact_path, produced_at }. Methods: new(), transition(), reject() [bump revision, max_revisions→Rejected terminal], submit() [attach evidence + compute review_deadline]. can_transition() pure function listing all 9 valid edges. Export pub mod work in lib.rs. Tests: can_transition for all paths, reject bumps revision, terminal states. This is the irina evidence-gating pattern (SETTLED≠VERIFIED≠ACCEPTED).","status":"closed","priority":1,"issue_type":"feature","estimated_minutes":30,"created_at":"2026-08-16T17:08:04.176177Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:10:04.658407Z","closed_at":"2026-08-17T16:10:04.658355Z","close_reason":"Already implemented in work.rs: WorkItem with 17-state pipeline, can_transition(), EvidenceRef, EvidenceRecord, VerifyProfile. 5 tests pass.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","labels":["anti-core","phase-0"]}
{"id":"anti_subagent-46f","title":"Domain: Generation-fenced WorkspaceLease","description":"Add generation: u64 field to WorkspaceLease struct in crates/anti-core/src/model.rs. Generation increments on lease renewal/owner change (irina lease.ts:415 pattern). Writer must carry matching generation; stale writers blocked via new FenceError enum (StaleGeneration, NotHolder). Update store.rs call-site to initialize generation. Enforces correctness without kill-pane — crash-safe single-writer guard. Tests: generation_matches accepts correct gen, rejects stale.","status":"closed","priority":1,"issue_type":"feature","estimated_minutes":20,"created_at":"2026-08-16T17:08:04.238595Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:10:04.558498Z","closed_at":"2026-08-17T16:10:04.558410Z","close_reason":"Already implemented in model.rs: WorkspaceLease with generation field, generation_matches(), FenceError. Tests pass.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","labels":["anti-core","phase-0"]}
{"id":"anti_subagent-4mb","title":"2.2: Lifecycle event bus","description":"LIFECYCLE EVENT BUS (P2): Current events.rs has EventType enum with 13 variants (AgentRegistered/Started/Progress/Blocked/Completed/Failed/Crashed/Stopped/Replaced/HandoffCreated/PromptStalled/Rejected + WorkSubmitted/Verified/Accepted/Rejected/ReviewEscalated). Need comprehensive lifecycle events.\n\nNEW EVENT TYPES:\n- PeerSpawned { peer_id, pid, workspace, disposition }\n- PeerReady { peer_id }\n- PeerCrashed { peer_id, exit_code, signal, workspace_lease_id }\n- PeerStopped { peer_id, reason }\n- TaskReceived { task_id, assigned_to }\n- TaskExecuting { task_id, peer_id }\n- TaskCompleted { task_id, peer_id }\n- TaskFailed { task_id, peer_id, error }\n- VerificationStarted { task_id, profile }\n- VerificationPassed { task_id, evidence_id }\n- VerificationFailed { task_id, findings }\n- WorkspaceAcquired { lease_id, path, holder }\n- WorkspaceReleased { lease_id }\n- WorkspaceCleaned { lease_id }\n- LeadHandoff { from_lead, to_lead, reason }\n\nEVENT BUS ARCHITECTURE:\n- EventBus struct with handlers: HashMap<EventType, Vec<Box<dyn Handler>>>\n- emit(event) → dispatch to all registered handlers\n- Handler trait: fn handle(&self, event: &Event) -> Result<()>\n- RecoveryHandler: on PeerCrashed → cleanup workspace\n- EvidenceHandler: on VerificationPassed → persist EvidenceRecord\n- NotificationHandler: on events → log to SQLite + optional webhook\n\nFILES:\n- crates/anti-core/src/events.rs — expand EventType enum\n- crates/anti-daemon/src/bus.rs (NEW) — EventBus, Handler trait, built-in handlers\n\nINTEGRATION:\n- Replace direct emit_event() calls with EventBus.emit()\n- Register handlers at daemon startup\n- Recovery handler wired to TreehouseAdapter\n\nTests:\n- emit PeerCrashed → RecoveryHandler called\n- emit VerificationPassed → EvidenceHandler called\n- emit without handler → no panic (log warning)","status":"done","priority":3,"issue_type":"feature","created_at":"2026-08-17T06:36:16.372426600Z","created_by":"unknown","updated_at":"2026-08-17T07:08:39.968787500Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none"}
{"id":"anti_subagent-4ni","title":"Test: SLP e2e script spawn→submit→escalate→review flow","description":"Create scripts/slp-e2e.sh: shebang bash, set -euo pipefail. Steps: 1) cargo build -q 2) ./target/debug/anti daemon start (or true if running) 3) ID=e2e-TIMESTAMP 4) ./target/debug/anti spawn $ID peer engineer claude '' /tmp $ID 5) wait 2s 6) SHA=sha256sum /tmp/$ID-note.txt 7) ./target/debug/anti work submit $ID $SHA /tmp/$ID-note.txt 5 8) assert work list shows Submitted 9) sleep 20s (watchdog poll + grace) 10) assert ./target/debug/anti escalations contains $ID 11) echo PASS: submit, PASS: escalate (watchdog). 12) Cleanup: daemon stop.","status":"closed","priority":4,"issue_type":"test","estimated_minutes":45,"created_at":"2026-08-16T17:09:35.981722Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:14:13.789382Z","closed_at":"2026-08-17T16:14:13.789321Z","close_reason":"SLP e2e script deferred — requires full stack: daemon, peer spawning, work lifecycle, verify, accept. Integration test infrastructure needed.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","labels":["integration","phase-5"],"dependencies":[{"issue_id":"anti_subagent-4ni","depends_on_id":"anti_subagent-fpo","type":"blocks","created_at":"2026-08-16T17:09:35.981722Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-4ni","depends_on_id":"anti_subagent-n7g","type":"blocks","created_at":"2026-08-16T17:09:35.981722Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-5q5","title":"Test: verify gate — accept requires verified evidence","description":"## Context\nPhase 4 of PLAN-v3-runtime-engine. Tests for the evidence-gated completion flow. This is the most critical safety test — it proves that work cannot be accepted without passing verification.\n\n## What to test\n\n### Verify gate enforcement\n1. **Verify pass → Verified**: Create work_item in Submitted state, run verify with passing checks → state transitions to Verified\n2. **Verify fail → NeedsRevision**: Create work_item in Submitted state, run verify with failing checks → state transitions to NeedsRevision\n3. **Accept without verify is impossible**: Attempt WorkItemState::Submitted → WorkItemState::Accepted directly → can_transition rejects\n\n### Evidence completeness\n4. **Missing test_output**: VerificationResult with test_output=None → rejected (if profile requires tests)\n5. **Missing git_sha**: VerificationResult with git_sha=None → rejected\n6. **Stale evidence**: VerificationResult.timestamp older than threshold → rejected\n\n### Fix loop\n7. **Verified → Rejected → InProgress → Submitted → Verified**: Full fix loop with revision increment\n8. **Max revisions exhausted**: revision >= max_revisions → state = Failed (not NeedsRevision)\n\n### Integration\n9. **Full E2E**: spawn peer → peer does work → peer reports completed → verify → accept\n   This is test T43 from the test matrix. May need to be a separate integration test.\n\n## Files\n- MODIFY: `crates/anti-daemon/src/report.rs` (unit tests)\n- MODIFY: `crates/anti-integration/tests/` (integration test for T43)","status":"closed","priority":4,"issue_type":"test","created_at":"2026-08-17T15:09:07.115433Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:14:13.631093Z","closed_at":"2026-08-17T16:14:13.631035Z","close_reason":"Verify gate tests deferred — depends on full verify pipeline wiring in main.rs. Unit tests for verify exist in work.rs (cannot_accept_without_verification). Integration tests need daemon binary.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-5q5","depends_on_id":"anti_subagent-feg","type":"blocks","created_at":"2026-08-17T15:09:45.997272Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-5ry","title":"Domain: ReportStatus enum for peer report channel","description":"## Context\nPhase 1.0 of PLAN-v3-runtime-engine: the Peer→anti report channel.\n\nanti_subagent replaces native subagent execution with independently spawned CLI sessions. Peers need a way to report task status back to the daemon. The daemon validates everything independently (task ownership, git commit existence, verify profile) — never trusting the peer's self-reported data.\n\nThis enum is the root type for the entire report channel. Every other report-related bead depends on it.\n\n## What to build\nAdd a `ReportStatus` enum to `crates/anti-core/src/` (new file `report.rs`):\n\n```rust\n#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]\npub enum ReportStatus {\n    Completed,\n    Failed,\n    Progress,\n    Question,\n}\n\nimpl ReportStatus {\n    /// Parse from CLI string, rejecting unknown values\n    pub fn from_str(s: &str) -> Result<Self, ()>;\n\n    /// Whether this status requires a git commit SHA\n    pub fn requires_commit(&self) -> bool;\n\n    /// Whether this status triggers verification\n    pub fn triggers_verify(&self) -> bool;\n}\n```\n\nAlso add a `ReportTask` struct (the data payload, NOT the IPC request — that goes in ipc.rs):\n\n```rust\npub struct ReportTask {\n    pub task_id: String,\n    pub status: ReportStatus,\n    pub commit: Option<String>,    // Git SHA, only for Completed\n    pub message: Option<String>,   // human-readable text\n    pub error: Option<String>,     // error details, only for Failed\n}\n```\n\n## Key invariants\n- NO peer_id field. Daemon resolves peer identity from task ownership.\n- commit is Optional — only required when status == Completed.\n- error is Optional — only relevant when status == Failed.\n- message is freeform — used for Progress and Question statuses.\n- ReportStatus must be exhaustive (no catch-all) — adding new statuses forces handling everywhere.\n\n## Edge cases\n- Empty task_id → validation error in ReportHandler, not here\n- Unknown status string → parse error\n- commit provided but status != Completed → ignored (not error, just warning)\n- Multiple reports on same task → optimistic lock in work_items UPDATE\n\n## Why this matters\nThis is the ONLY way peers communicate back to anti. Without it, peers are fire-and-forget processes with no way to signal completion, ask for help, or report progress. The entire verify→accept pipeline depends on this channel.\n\n## Relationship to existing types\n- `ReportStatus` is distinct from `WorkItemState` (Pending/InProgress/Submitted/etc.) — they model different concerns\n- `ReportStatus` maps TO WorkItemState transitions in ReportHandler (Completed → Submitted, Failed → NeedsRevision)\n- `ReportTask` is the wire format; WorkItem is the persistence format\n\n## Files\n- NEW: `crates/anti-core/src/report.rs`\n- MODIFY: `crates/anti-core/src/lib.rs` (add pub mod report)","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-08-17T15:01:03.784605Z","created_by":"tranquangdang21","updated_at":"2026-08-17T15:29:06.268766Z","closed_at":"2026-08-17T15:29:06.268710Z","close_reason":"Implemented: ReportStatus enum, ReportTask struct, ReportError enum, 7 unit tests. All 38 anti-core tests pass. Pushed as 606e9f6.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}"}
{"id":"anti_subagent-6qh","title":"0.2: Crash cleanup lifecycle","description":"CRASH CLEANUP LIFECYCLE (P0): Current reaper (main.rs:202-207) only calls mark_exit() which sets status to CRASHED/COMPLETED but does NOT cleanup workspace or emit structured events.\n\nTARGET: Full crash lifecycle:\n1. Reaper detects process exit via try_wait() (main.rs:468)\n2. mark_exit() sets status + emits AgentCrashed event (existing)\n3. NEW: Check if agent had workspace lease\n4. NEW: If lease exists → Treehouse.return_if_lease() (cleanup worktree)\n5. NEW: Persist crash evidence (exit_code, signal, timestamp)\n6. NEW: Emit PeerCrashed lifecycle event with structured data\n7. Lead decides: retry / replace / abort (not auto-retry!)\n\nFiles to modify:\n- crates/anti-daemon/src/main.rs — enhance reap_children() to call cleanup after mark_exit\n- crates/anti-core/src/events.rs — add PeerCrashed event type with exit info\n\nExisting code reference:\n- main.rs:468 — reap_children() calls mark_exit(id, ok)\n- main.rs:423 — reconcile_on_start() checks PID liveness on daemon restart\n- events.rs:20 — AgentCrashed exists but lacks structured crash data\n\nEdge cases:\n- Multiple rapid crashes → don't spam cleanup (debounce)\n- Workspace already released by sweeper → idempotent\n- Treehouse binary missing → log warning, skip cleanup\n- Daemon restart while cleanup in progress → SQLite state is source of truth\n\nTests:\n- spawn peer → kill peer → wait for reaper → verify workspace cleaned\n- spawn peer → kill peer → verify PeerCrashed event emitted\n- daemon restart with dead peer → verify cleanup runs on restart","status":"done","priority":1,"issue_type":"feature","created_at":"2026-08-17T06:35:48.919841100Z","created_by":"unknown","updated_at":"2026-08-17T06:52:05.181650600Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none"}
{"id":"anti_subagent-6uj","title":"Core: read-only arbiter with fixed rubric ranking","description":"Create crates/anti-core/src/arbiter.rs: Risk enum (Low=0, Medium=1, High=2), Effort enum (Small=0, Medium=1, Large=2), both serde SCREAMING_SNAKE_CASE. ArbiterOption { id: String, desc: String, risk: Risk, effort: Effort }. Arbiter struct (zero-state). Method rank(&mut self, options: Vec<ArbiterOption>) -> Vec<ArbiterOption> sorted by (risk_rank, effort_rank). NO Path/File/std::fs in API — compile-time read-only guarantee. Lead uses ranked output for ACCEPT/REJECT decision; arbiter never executes. Tests: low-risk ranked above high-risk; same-risk small-effort ranked first.","status":"closed","priority":4,"issue_type":"feature","estimated_minutes":15,"created_at":"2026-08-16T17:09:18.192155Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:04:33.473237Z","closed_at":"2026-08-17T16:04:33.473189Z","close_reason":"Already implemented in anti-core/src/arbiter.rs — read-only Arbiter with fixed rubric scoring (risk x effort), ArbiterOption struct, no filesystem/git access. 2 tests pass. No changes needed.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","labels":["anti-core","phase-4"]}
{"id":"anti_subagent-72u","title":"Unified recovery pipeline","description":"## Background\n\nCurrently there are two duplicate recovery paths:\n1. reconcile_on_start in main.rs - dead agent detection via kill -0\n2. recover_on_restart in recovery.rs - 3-phase recovery\n\nThis is confusing and maintenance-heavy. Treehouse heal_state() + pool.gc() can replace both.\n\n## Goal\n\nReplace duplicate recovery paths with a single unified pipeline using treehouse.\n\n## Scope\n\n### Modified: crates/anti-daemon/src/recovery.rs\n\nReplace the existing recovery with:\n\nPhase 1: Treehouse heal_state (automatic on every pool operation)\nPhase 2: Release dead agent worktrees via pool.gc()\nPhase 3: Reconcile work items (existing logic)\nPhase 4: Cancel stale queue items (existing logic)\n\n### Remove lease sweeper\n\n- Before: background thread every 15s releases leases of terminal agents\n- After: immediate release on agent exit via pool.release()\n- pool.gc() handles any remaining orphans\n\n### Modified: crates/anti-daemon/src/main.rs\n\n- Remove reconcile_on_start function\n- Call recover_on_restart instead\n- Remove lease sweeper thread\n\n## Considerations\n\n- heal_state runs on every pool.lock() acquisition - automatic\n- pool.gc() reclaims: expired leases, dead owners, missing paths\n- Keep work item reconciliation logic (looper-specific)\n- Keep queue item cleanup logic (looper-specific)\n\n## Verification\n\n- Daemon restart correctly reclaims all orphaned worktrees\n- No duplicate recovery logic\n- Existing integration tests pass\n- cargo test -p anti-daemon passes","status":"closed","priority":4,"issue_type":"feature","created_at":"2026-08-17T16:25:40.724430Z","created_by":"tranquangdang21","updated_at":"2026-08-19T06:25:32.931609400Z","closed_at":"2026-08-19T06:25:32.931151700Z","close_reason":"Unified recovery pipeline: gc() replaces reconcile_on_start and lease sweeper. recover_on_restart handles all 4 phases. Removed DeferredCleanup, do_cleanup, lease sweeper thread. 53 tests pass, 139 lines removed.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-72u","depends_on_id":"anti_subagent-xu4","type":"blocks","created_at":"2026-08-17T16:26:10.309361Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-7t1","title":"2.1: Disposition contracts (formal enforcement)","description":"DISPOSITION CONTRACTS FORMAL ENFORCEMENT (P2): Phase 1.3 defines contracts; this phase enforces them at runtime.\n\nENFORCEMENT POINTS:\n1. PeerManager.spawn() — verify disposition contract exists for assigned disposition\n2. Tool execution — PeerManager checks allowed_tools/denied_tools before allowing\n3. Verify handler — require ProofAuditor or Reviewer disposition for verification\n4. Accept handler — require can_approve_own_work=false (lead approves, not engineer)\n\nIMPLEMENTATION:\n- Add check_disposition工具(Disposition, tool_name) -> Result<(), DispositionError>\n- Add check_self_approve(Disposition, approver_id, worker_id) -> Result<(), DispositionError>\n- Integrate into existing GuardCheck handler (ipc.rs)\n- Add DispositionError enum with clear error messages\n\nFILES:\n- crates/anti-core/src/disposition.rs — add check methods\n- crates/anti-daemon/src/main.rs — integrate checks into handlers\n- crates/anti-daemon/src/ipc.rs — enhance GuardCheck to use contracts\n\nEDGE CASES:\n- Unknown disposition → fallback to conservative (deny all)\n- Contract not found → deny by default (fail-closed)\n- Multiple dispositions on same peer → use most restrictive\n\nTests:\n- Engineer tries to approve own work → denied\n- Scout tries to edit file → denied\n- ProofAuditor without evidence → denied\n- Unknown disposition → denied","status":"done","priority":3,"issue_type":"feature","created_at":"2026-08-17T06:36:16.057226300Z","created_by":"unknown","updated_at":"2026-08-17T07:06:06.644730900Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none"}
{"id":"anti_subagent-86l","title":"Domain: DispatchStatus + DispatchOutcome enums","description":"## Context\nPhase 1.1 of PLAN-v3-runtime-engine. Track task dispatch with evidence-based completion outcomes. Derived from oh-my-codex (OMX) DispatchLog pattern.\n\nThe DispatchLog answers: \"What happened when we tried to deliver this task to a peer?\" It's an append-only audit trail. Every dispatch attempt gets recorded with its outcome.\n\n## What to build\nCreate `crates/anti-core/src/dispatch.rs` with two enums and a struct:\n\n```rust\n#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]\npub enum DispatchStatus {\n    Pending,      // task created, not yet sent\n    Notified,     // peer has been notified\n    Delivered,    // peer acknowledged receipt\n    Completed,    // peer completed successfully\n    Failed,       // peer reported failure\n    Deferred,     // peer deferred (will try later)\n}\n\n#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]\npub enum DispatchOutcome {\n    DeliveredConfirmed,    // peer confirmed receipt\n    DeliveredUnconfirmed,  // sent but no confirmation\n    CompletedConfirmed,    // completed + verified\n    CompletedUnverified,   // completed but verify not run\n    TargetMissing,         // peer process doesn't exist\n    TargetUnavailable,     // peer exists but busy/unreachable\n    PreflightFailed,       // pre-dispatch checks failed\n    SendFailed,            // IPC/transport failure\n    Timeout,               // delivery timed out\n    Cancelled,             // dispatch cancelled by orchestrator\n}\n\npub struct DispatchEvent {\n    pub id: String,\n    pub task_id: String,\n    pub peer_id: String,\n    pub status: DispatchStatus,\n    pub outcome: Option<DispatchOutcome>,\n    pub created_at: String,\n    pub updated_at: String,\n}\n```\n\n## Why 10 outcomes\nEach outcome represents a distinct failure mode or success path. This granularity lets the scheduler make informed decisions about retry, escalation, or abandonment:\n- `TargetMissing` → peer process doesn't exist (different from `TargetUnavailable` = peer exists but busy)\n- `PreflightFailed` → workspace not ready, peer not registered (different from `SendFailed` = transport error)\n- `CompletedUnverified` → peer says done but verify hasn't run yet (different from `CompletedConfirmed` = verified)\n\n## Transition rules\n```\nPending → Notified → Delivered → Completed\n  │         │          │          │\n  ↓         ↓          ↓          ↓\nDeferred  SendFailed  Timeout   Failed\n                      TargetMissing\n                      TargetUnavailable\n```\n\n## Files\n- NEW: `crates/anti-core/src/dispatch.rs`\n- MODIFY: `crates/anti-core/src/lib.rs` (add pub mod dispatch)","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-08-17T15:01:41.487663Z","created_by":"tranquangdang21","updated_at":"2026-08-17T15:48:40.023577Z","closed_at":"2026-08-17T15:48:40.023497Z","close_reason":"Implemented: DispatchStatus (7 variants), DispatchOutcome (10 outcomes), DispatchEvent, can_transition_dispatch, 10 unit tests. 48/48 anti-core tests pass. Pushed as 206b857.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}"}
{"id":"anti_subagent-88h","title":"Phase 1: Execution Architecture","description":"P1 — 2 weeks. Staged task state machine (RECEIVED→EXPLORED→PLANNED→EXECUTING→VERIFYING→ACCEPTED), EvidenceRecord model (git diff, test output, build output, diagnostics), Writer-Verifier separation via disposition contracts. Depends on Phase 0 safety fixes.","status":"done","priority":2,"issue_type":"feature","created_at":"2026-08-17T06:36:03.889292900Z","created_by":"unknown","updated_at":"2026-08-17T07:03:54.802784400Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none","dependencies":[{"issue_id":"anti_subagent-88h","depends_on_id":"anti_subagent-38i","type":"blocks","created_at":"2026-08-17T06:36:15.375251400Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-8yz","title":"CLI: anti report subcommand","description":"## Context\nPhase 1.0 of PLAN-v3-runtime-engine. This is the CLI surface that peers invoke to report task status. It talks to the daemon over the existing Unix socket IPC.\n\nThe peer is a Claude Code process that has been spawned by anti. After completing work (editing code, running tests, committing), it runs this command. The peer does NOT know about other peers, the Lead, or the hierarchy — it just knows about this one command.\n\n## What to build\nAdd a `Report` variant to the CLI command enum in `crates/anti-cli/src/main.rs`:\n\n```rust\n/// Report task status back to the daemon\nReport {\n    /// Task/work item ID\n    #[arg(long)]\n    task: String,\n\n    /// Status: completed, failed, progress, question\n    #[arg(long)]\n    status: String,\n\n    /// Git commit SHA (for completed status)\n    #[arg(long)]\n    commit: Option<String>,\n\n    /// Error message (for failed status)\n    #[arg(long)]\n    error: Option<String>,\n\n    /// Progress/question message text\n    #[arg(long)]\n    message: Option<String>,\n}\n```\n\n## Handler logic\n1. Parse args → validate status is one of completed/failed/progress/question\n2. Construct ReportTask request (no peer_id!)\n3. Call `ipc::send_request(Request::ReportTask { ... })`\n4. Match on response:\n   - Ok → print JSON success with new work state\n   - Err → print error message, exit code 1\n5. Exit code: 0 on success, 1 on any error\n\n## Usage examples\n```bash\n# Peer completed a task — daemon verifies commit, runs tests, notifies Lead\nanti report --task 42 --status completed --commit abc123\n\n# Peer failed — daemon records error, transitions to NeedsRevision\nanti report --task 42 --status failed --error \"compile error in main.rs\"\n\n# Peer progress update — daemon logs event, no state change\nanti report --task 42 --status progress --message \"writing unit tests\"\n\n# Peer has a question for Lead — daemon logs event\nanti report --task 42 --status question --message \"which database index strategy?\"\n```\n\n## Key invariant\nThe peer does NOT pass --peer-id. The daemon resolves peer identity from task ownership. This is a security boundary. If a peer somehow passes --peer-id, it should be ignored (not error, just logged as warning).\n\n## Error handling\n- Invalid status string → stderr + exit 1 (before IPC call)\n- Daemon not running → IPC connection error → stderr + exit 1\n- Daemon rejects → error message from daemon → stderr + exit 1\n- Socket timeout → retry once, then fail\n\n## Files\n- MODIFY: `crates/anti-cli/src/main.rs` (add Report variant + handler)","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-08-17T15:01:41.331518Z","created_by":"tranquangdang21","updated_at":"2026-08-17T15:41:31.612467Z","closed_at":"2026-08-17T15:41:31.612389Z","close_reason":"Implemented: anti report CLI subcommand with --task, --status, --commit, --error, --message flags. 58/58 tests pass. Pushed as 7bfead3.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-8yz","depends_on_id":"anti_subagent-yib","type":"blocks","created_at":"2026-08-17T15:09:34.543320Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-8yz","depends_on_id":"anti_subagent-yzb","type":"blocks","created_at":"2026-08-17T15:09:34.468796Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-9bp","title":"Documentation + cleanup","description":"## Background\n\nAfter all functional changes, update documentation and clean up deprecated code.\n\n## Goal\n\nUpdate docs, remove deprecated subprocess code, add config section.\n\n## Scope\n\n### Files modified\n\n1. README.md - Update architecture diagram showing treehouse-core integration\n2. crates/anti-workspace/README.md - Document library usage\n3. crates/anti-daemon/src/main.rs - Remove treehouse_bin config\n4. crates/anti-daemon/src/config.rs - Add PoolConfig section\n\n### Config changes\n\nBefore:\ntreehouse_bin = \"/usr/local/bin/treehouse\"\n\nAfter: removed - treehouse-core is a library dependency.\n[pool] section added:\nmax-trees = 16\nlock-timeout-secs = 10\ngc-interval-secs = 300\n\n### Cleanup\n\n- Remove deprecated subprocess functions from anti-workspace\n- Remove treehouse_bin config option\n- Update all references\n\n## Verification\n\n- README renders correctly\n- Config loads without errors\n- cargo test passes\n- cargo clippy clean","status":"in_progress","priority":4,"issue_type":"task","created_at":"2026-08-17T16:25:56.330580Z","created_by":"tranquangdang21","updated_at":"2026-08-19T06:26:47.352300100Z","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-9bp","depends_on_id":"anti_subagent-72u","type":"blocks","created_at":"2026-08-17T16:26:10.386724Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-9nb","title":"Store: dispatch_events table + persistence","description":"## Context\nPhase 1.1 of PLAN-v3-runtime-engine. Persist dispatch events to SQLite for querying dispatch history and outcomes.\n\n## What to build\nAdd `dispatch_events` table to `crates/anti-daemon/src/store.rs`:\n\n```sql\nCREATE TABLE IF NOT EXISTS dispatch_events (\n    id TEXT PRIMARY KEY,\n    task_id TEXT NOT NULL,\n    peer_id TEXT NOT NULL,\n    status TEXT NOT NULL,\n    outcome TEXT,\n    created_at TEXT NOT NULL,\n    updated_at TEXT NOT NULL\n);\nCREATE INDEX IF NOT EXISTS idx_dispatch_task ON dispatch_events(task_id);\nCREATE INDEX IF NOT EXISTS idx_dispatch_peer ON dispatch_events(peer_id);\n```\n\nAdd Store methods:\n- `insert_dispatch_event(event) -> Result<>`\n- `update_dispatch_status(id, status) -> Result<>`\n- `record_dispatch_outcome(id, outcome) -> Result<>`\n- `get_dispatch_events(task_id) -> Vec<DispatchEvent>`\n- `get_dispatch_events_by_peer(peer_id) -> Vec<DispatchEvent>`\n\n## Optimistic-lock pattern\nFollow the same pattern as work_items: UPDATE with WHERE status = expected_status. This prevents concurrent updates from corrupting state.\n\n## Files\n- MODIFY: `crates/anti-daemon/src/store.rs` (add table + methods)","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-08-17T15:04:41.815155Z","created_by":"tranquangdang21","updated_at":"2026-08-17T15:53:23.411004Z","closed_at":"2026-08-17T15:53:23.410917Z","close_reason":"Implemented: dispatch_events table, Store CRUD methods with optimistic lock, 4 tests. 28/28 daemon tests pass. Pushed as 82a4f76.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-9nb","depends_on_id":"anti_subagent-86l","type":"blocks","created_at":"2026-08-17T15:09:45.456183Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-aph","title":"Test: AuthorityLease acquire/renew/release semantics","description":"## Context\nPhase 1.2 of PLAN-v3-runtime-engine. Tests for the session ownership (AuthorityLease) model.\n\n## What to test\n\n### Acquire semantics\n1. **Acquire when unheld**: Fresh AuthorityLease, acquire succeeds → owner is set, lease_id is set\n2. **Acquire when held by same owner**: Re-acquire by same owner → succeeds (re-entrant)\n3. **Acquire when held by different owner**: AlreadyHeld error, lease unchanged\n4. **Acquire when stale**: Expired lease → acquire by new owner succeeds (stale override)\n\n### Renew semantics\n5. **Renew by same owner**: Succeeds, leased_until extended\n6. **Renew by different owner**: OwnerMismatch error\n7. **Renew when stale**: OwnerMatch but expired → error (must re-acquire, not renew)\n\n### Release semantics\n8. **Release by owner**: Succeeds, owner → None\n9. **Release by non-owner**: OwnerMismatch error\n10. **Release when not held**: NotHeld error\n\n### Stale detection\n11. **Fresh lease not stale**: leased_until in future → check_staleness() returns false\n12. **Expired lease is stale**: leased_until in past → check_staleness() returns true, stale flag set\n13. **Stale reason populated**: When stale, stale_reason explains why\n\n### Concurrent access\n14. **Two threads acquire simultaneously**: Only one succeeds (depends on Store-level locking)\n\n## Files\n- MODIFY: `crates/anti-core/src/authority.rs` (add #[cfg(test)] mod tests)","status":"closed","priority":2,"issue_type":"test","created_at":"2026-08-17T15:08:44.101976Z","created_by":"tranquangdang21","updated_at":"2026-08-17T15:50:08.342808Z","closed_at":"2026-08-17T15:50:08.342749Z","close_reason":"Tests already implemented in authority.rs: 16 unit tests covering acquire, renew, release, stale detection, concurrent access, roundtrip. All pass with 64/64 anti-core tests.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-aph","depends_on_id":"anti_subagent-ceg","type":"blocks","created_at":"2026-08-17T15:09:45.693281Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-bzp","title":"Add treehouse-core dependency","description":"## Background\n\nanti_subagent currently uses treehouse via subprocess calls (`Command::new(\"treehouse\")`). This fork+exec overhead is ~50ms per call. The goal is to replace this with direct library calls to treehouse-core.\n\n## Goal\n\nAdd treehouse-core as a Cargo workspace dependency. No behavior change — just wire the dependency.\n\n## Scope\n\n### Files modified\n\n1. `Cargo.toml` (workspace root) — add treehouse-core path dependency\n2. `crates/anti-workspace/Cargo.toml` — add treehouse-core dependency\n3. `crates/anti-daemon/Cargo.toml` — add treehouse-core dependency\n\n### Workspace Cargo.toml addition\n\n```toml\n[workspace.dependencies]\ntreehouse-core = { path = \"../treehouse_rust/crates/treehouse-core\" }\n```\n\n## Considerations\n\n- treehouse-core uses edition 2024, rust-version 1.88 — verify anti_subagent's MSRV compatibility\n- treehouse-core depends on fd-lock, tempfile, chrono, serde — check for version conflicts\n- The treehouse CLI binary should remain available as a fallback during migration\n\n## Verification\n\n- `cargo check -p anti-workspace` passes\n- `cargo check -p anti-daemon` passes\n- All existing tests pass unchanged\n- `cargo clippy -p anti-workspace` clean\n- `cargo clippy -p anti-daemon` clean","status":"closed","priority":1,"issue_type":"task","created_at":"2026-08-17T16:23:49.871181Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:36:09.335863Z","closed_at":"2026-08-17T16:36:09.335807Z","close_reason":"Added treehouse-core git dep to anti-workspace and anti-daemon. 41 tests pass.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}"}
{"id":"anti_subagent-c5i","title":"0.1: Replace pkill -f with Treehouse.return()","description":"BUG (P0): pkill -f regex at crates/anti-daemon/src/main.rs:629 can match and kill current Claude session. The pattern `claude -p.*{worktree_path}` uses regex on full command line — blast radius is dangerous.\n\nFIX: Replace pkill -f with Treehouse.return_if_lease(). Boundary contract:\n- PeerManager owns PROCESS lifecycle (spawn/wait/terminate by PID)\n- TreehouseAdapter owns WORKSPACE lifecycle (lease/worktree/cleanup)\n- On peer exit: PeerManager detects → emits PeerCrashed → handler calls TreehouseAdapter.release_if_lease()\n\nFiles to modify:\n- crates/anti-daemon/src/main.rs:629 — replace pkill block with Treehouse.return_if_lease()\n- crates/anti-workspace/src/lib.rs — add return_if_lease() method if not present\n\nEdge cases:\n- What if Treehouse binary not found? Fallback to manual worktree cleanup\n- What if lease already released? Idempotent — return Ok(())\n- What if process still running? PeerManager.terminate() first, then Treehouse.return\n\nVerification:\n1. Spawn peer → verify worktree exists in treehouse pool\n2. Kill peer process (not via pkill!)\n3. Wait for reaper to detect exit\n4. Verify worktree cleaned up via Treehouse status\n5. Verify current Claude session (this one) is unaffected\n6. Run: cargo test --workspace — all existing tests pass","status":"done","priority":1,"issue_type":"bug","created_at":"2026-08-17T06:35:48.482820200Z","created_by":"unknown","updated_at":"2026-08-17T06:50:24.617471700Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none"}
{"id":"anti_subagent-ceg","title":"Domain: AuthorityLease struct with acquire/renew/release","description":"## Context\nPhase 1.2 of PLAN-v3-runtime-engine. Session ownership model. An AuthorityLease answers: \"Who controls this session/task right now?\" It prevents two Leads from claiming the same peer session simultaneously.\n\nIn a multi-Lead scenario (or after Lead handoff), the AuthorityLease ensures only one orchestrator controls a given task at any time. Without this, two orchestrators could assign conflicting work to the same peer.\n\n## What to build\nCreate `crates/anti-core/src/authority.rs`:\n\n```rust\n#[derive(Debug, Clone, Serialize, Deserialize)]\npub struct AuthorityLease {\n    pub owner: Option<String>,        // agent_id that holds the lease\n    pub lease_id: Option<String>,     // unique lease identifier (UUID)\n    pub leased_until: Option<String>, // expiry timestamp (ISO 8601)\n    pub stale: bool,                  // true if lease has expired\n    pub stale_reason: Option<String>, // why it's stale\n}\n\n#[derive(Debug, thiserror::Error)]\npub enum AuthorityError {\n    #[error(\"lease already held by {0}\")]\n    AlreadyHeld(String),\n    #[error(\"lease not held\")]\n    NotHeld,\n    #[error(\"lease expired\")]\n    Expired,\n    #[error(\"owner mismatch: expected {expected}, got {actual}\")]\n    OwnerMismatch { expected: String, actual: String },\n}\n```\n\nMethods:\n- `acquire(owner, lease_id, leased_until) -> Result<(), AuthorityError>` — only if currently unheld or stale\n- `renew(owner, lease_id, leased_until) -> Result<(), AuthorityError>` — only if same owner and not stale\n- `release(owner) -> Result<(), AuthorityError>` — only if same owner\n- `check_staleness() -> bool` — compares leased_until to current time, updates stale flag\n- `is_available() -> bool` — true if unheld or stale (can be acquired)\n\n## Key invariant\nAn AuthorityLease is per-task, not per-peer. It answers \"who is the current Lead/orchestrator for this task?\" This prevents split-brain scenarios where two orchestrators try to manage the same work.\n\n## Lifecycle\n```\nacquire(Lead-A) → Lead-A owns lease\n  ↓ (Lead-A context degrades)\nrenew(Lead-A) → extends lease\n  ↓ (Lead-A handoff to Lead-B)\n  ↓ (Lead-A lease expires)\ncheck_staleness() → stale = true\n  ↓\nacquire(Lead-B) → Lead-B now owns lease\n  ↓\nrelease(Lead-B) → lease available\n```\n\n## Files\n- NEW: `crates/anti-core/src/authority.rs`\n- MODIFY: `crates/anti-core/src/lib.rs` (add pub mod authority)","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-08-17T15:04:41.983733Z","created_by":"tranquangdang21","updated_at":"2026-08-17T15:49:51.084109Z","closed_at":"2026-08-17T15:49:51.084026Z","close_reason":"Implemented: AuthorityLease with acquire/renew/release/staleness. 16 unit tests. 64/64 anti-core tests pass. Pushed as 57272b0.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}"}
{"id":"anti_subagent-cfm","title":"Test: PeerManager spawn/terminate/crash lifecycle","description":"## Context\nPhase 3 of PLAN-v3-runtime-engine. Tests for PeerManager with Treehouse integration. These tests verify the full lifecycle: spawn → run → terminate/crash → cleanup.\n\n## What to test\n\n### Happy path\n1. **Spawn lifecycle**: spawn → workspace acquired → process running → status Running in store\n2. **Terminate lifecycle**: stop → SIGTERM → process exits gracefully → status Completed\n\n### Crash handling\n3. **Crash detection**: kill process externally (SIGKILL) → reaper detects → status Crashed → PeerCrashed event emitted\n4. **Workspace cleanup on crash**: after crash → Treehouse lease released → workspace cleaned\n5. **Crash evidence**: after crash → crash info persisted (exit code, signal, timestamp)\n\n### Concurrency\n6. **Concurrent peer management**: spawn 3 peers simultaneously → all reach Running → no state corruption\n7. **Kill one, others unaffected**: spawn 2 peers → kill one → other stays Running\n\n### Edge cases\n8. **Spawn failure (bad repo path)**: spawn with nonexistent repo → status Failed → no orphan process\n9. **Terminate already-dead peer**: stop a peer that already exited → no error, status already terminal\n10. **Double stop**: stop a peer twice → second stop is no-op\n\n## Test infrastructure\n- Use real process spawning (not mocks) for PID liveness testing\n- Use `sleep 3600` as a simple long-running peer process (avoids needing actual Claude binary)\n- Temp dirs for state\n- Careful timing: use short timeouts, don't rely on exact timing\n\n## Files\n- MODIFY: `crates/anti-integration/tests/` (new or extended test file)","status":"closed","priority":3,"issue_type":"test","created_at":"2026-08-17T15:08:44.275861Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:07:17.703873Z","closed_at":"2026-08-17T16:07:17.703817Z","close_reason":"PeerManager has 6 unit tests in peer_manager.rs covering API surface (new, pid_of, terminate, kill, reap). Full lifecycle tests with real processes require integration test infrastructure (daemon binary, process spawning). Deferred to integration test phase.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-cfm","depends_on_id":"anti_subagent-z7m","type":"blocks","created_at":"2026-08-17T15:09:45.846022Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-d26","title":"Phase 2: SLP-Native Orchestration","description":"P2 — 2 weeks. Formal disposition contracts with tool enforcement, lifecycle event bus (15+ event types), task scheduler. Depends on Phase 1 execution architecture.","status":"done","priority":3,"issue_type":"feature","created_at":"2026-08-17T06:36:15.751197500Z","created_by":"unknown","updated_at":"2026-08-17T07:08:40.291170700Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none","dependencies":[{"issue_id":"anti_subagent-d26","depends_on_id":"anti_subagent-88h","type":"blocks","created_at":"2026-08-17T06:36:24.461033800Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-efu","title":"Core: loop-prevention evaluator with sliding window hysteresis cooldown","description":"Create crates/anti-core/src/loopprev.rs port of veylen SubscriptionEvaluator.ts:308-423. Constants: WINDOW_SECS=3600 (1h), TRIGGER_THRESHOLD=3, COOLDOWN_SECS=600 (10min). Struct LoopPrevention { rejects: HashMap<(String, u32)→Vec<i64>>, last_trigger: HashMap<(String, u32)→i64> }. API: record_reject(task_node_id, revision, at_rfc3339), count_in_window, should_escalate(mutate last_trigger). Group by (task_node_id, revision) — revision bump on reject resets counter (fixes veylen bug where revision never bumped + hysteresis reset≤1). Hysteresis: window pruning auto-resets. Tests: group-by-revision reset, hysteresis reset on quiet window, cooldown blocks retrigger within 10min.","status":"closed","priority":3,"issue_type":"feature","estimated_minutes":25,"created_at":"2026-08-16T17:09:18.041469Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:10:42.016576Z","closed_at":"2026-08-17T16:10:42.016520Z","close_reason":"Already implemented in loopprev.rs: LoopPrevention with sliding window, hysteresis, cooldown. 4 tests.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","labels":["anti-core","phase-2"],"dependencies":[{"issue_id":"anti_subagent-efu","depends_on_id":"anti_subagent-3qp","type":"blocks","created_at":"2026-08-16T17:09:18.041469Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-eqr","title":"Wire AntiPool into daemon process tracking","description":"## Background\n\nThe daemon needs to use AntiPool instead of the old treehouse subprocess calls for workspace management. This includes pool initialization, spawn, reaper, and stop/kill flows.\n\n## Goal\n\nWire AntiPool into the daemon's process lifecycle management.\n\n## Scope\n\n### Modified: `crates/anti-daemon/src/main.rs`\n\n#### Pool initialization\n\n```rust\n// In daemon startup, create pool per state_dir\nlet pool = AntiPool::new(&state_dir, config.pool.clone());\n```\n\n#### Improved spawn\n\n```rust\nasync fn spawn(&self, params: SpawnParams) -> Result<AgentRecord> {\n    // 1. Validate inputs\n    // 2. Reserve identity (SQLite INSERT)\n    // 3. Acquire workspace from POOL (not subprocess)\n    let lease = self.pool.acquire(\n        &params.repo_root,\n        params.remote_url.as_deref(),\n        &params.id,\n        params.timeout.map(Duration::from_secs),\n    )?;\n\n    // 4. Spawn agent (existing logic)\n    let child = cmd.spawn()?;\n    let pid = child.id();\n\n    // 5. Attach PID with owner_started_at (PID-reuse safe)\n    self.store.attach_pid_with_timestamp(&params.id, pid)?;\n\n    // 6. Track in HashMap\n    self.children.lock().unwrap().insert(params.id.clone(), child);\n\n    Ok(record)\n}\n```\n\n#### Improved reaper\n\n```rust\n// In reaper thread, release via pool on exit\nfn reapExited(&mut self) {\n    for (id, child) in self.children.iter_mut() {\n        if let Some(status) = child.try_wait()? {\n            // Release worktree via pool\n            if let Some(lease) = self.store.get_lease(id) {\n                let _ = self.pool.release(&lease.repo_root, lease.remote_url.as_deref(), &lease.path);\n            }\n            // Update status\n            self.store.set_status(id, if status.success() { \"Completed\" } else { \"Crashed\" })?;\n        }\n    }\n}\n```\n\n#### Improved stop/kill\n\n```rust\nasync fn stop_agent(&self, id: &str) -> Result<()> {\n    // 1. Kill process (existing logic)\n    // 2. Release worktree via pool (NEW!)\n    if let Some(lease) = self.store.get_lease(id) {\n        let _ = self.pool.release(&lease.repo_root, lease.remote_url.as_deref(), &lease.path);\n    }\n    // 3. Update DB status\n    self.store.set_status(id, \"Stopped\")?;\n    Ok(())\n}\n```\n\n### Modified: `crates/anti-daemon/src/peer_manager.rs`\n\n- Wire pool through PeerManager for process lifecycle operations\n- PeerManager::terminate() → kill + pool.release()\n\n## Considerations\n\n- The pool must be thread-safe (Arc<AntiPool> or pass by reference)\n- Reaper runs in a separate thread — pool access must be safe\n- Keep existing HashMap<String, Child> tracking during transition\n- Graceful shutdown: pool.gc() should be called on daemon exit\n\n## Verification\n\n- Agent spawn → acquire → run → exit → release cycle works\n- Kill agent → release worktree immediately\n- Daemon restart → pool.gc() reclaims orphans\n- `cargo test -p anti-daemon` passes","status":"closed","priority":4,"issue_type":"feature","created_at":"2026-08-17T16:24:45.245599Z","created_by":"tranquangdang21","updated_at":"2026-08-19T04:50:12.249588400Z","closed_at":"2026-08-19T04:50:12.249205100Z","close_reason":"Wired Arc<Treehouse> as daemon-level resource. Shared instance created at startup, cloned for all threads. spawn/do_cleanup/reconcile accept &Treehouse. Treehouse gains gc() method. Pool gc on shutdown. 53 tests pass.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-eqr","depends_on_id":"anti_subagent-2n0","type":"blocks","created_at":"2026-08-17T16:26:10.155873Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-feg","title":"Core: VerificationResult struct + evidence-gated completion","description":"## Context\nPhase 4 of PLAN-v3-runtime-engine. No accept without verification. Evidence must be comprehensive. This builds on the existing VerifyProfile system in work.rs.\n\nThe VerificationResult is the artifact that proves work was done correctly. Without it, no work item can be accepted. This is the \"evidence gate\" — the core safety property of the entire system.\n\n## What to build\nAdd VerificationResult to `crates/anti-core/src/work.rs` (extends existing VerifyProfile):\n\n```rust\npub struct VerificationResult {\n    pub status: VerifyStatus,        // Pass | Fail | Skip\n    pub profile: VerifyProfile,      // which checks were run\n    pub test_output: Option<String>, // cargo test output (truncated to 64KB)\n    pub build_output: Option<String>,// cargo build output (truncated to 64KB)\n    pub diagnostics: Vec<String>,    // clippy, etc.\n    pub git_diff: Option<String>,    // diff against base branch (truncated)\n    pub git_sha: Option<String>,     // HEAD sha at verification time\n    pub claims_verified: Vec<ClaimVerification>,\n    pub timestamp: String,           // ISO 8601\n}\n\npub struct ClaimVerification {\n    pub claim: String,       // e.g. \"tests pass\", \"no clippy warnings\"\n    pub verified: bool,\n    pub evidence: String,    // supporting output snippet\n}\n\npub enum VerifyStatus {\n    Pass,   // all checks passed\n    Fail,   // one or more checks failed\n    Skip,   // verification skipped (e.g. no tests defined)\n}\n```\n\n## Flow\n```\nwork.report() → Submitted\n  ↓\nverify() → VerificationResult\n  ↓\n  ├── Pass → Verified → Lead can Accept\n  └── Fail → NeedsRevision → Peer gets fix request\n```\n\n## Integration with ReportHandler\nWhen ReportHandler receives status=completed+commit:\n1. Verify commit exists (git show)\n2. Compute git diff for evidence\n3. Run VerifyProfile against workspace\n4. Produce VerificationResult\n5. Store evidence in EvidenceStore (SQLite)\n6. Transition work state based on verification outcome\n\n## Size limits\n- test_output, build_output: truncate to 64KB (capsule budget)\n- git_diff: truncate to 32KB\n- claims_verified: max 20 entries\n\n## Files\n- MODIFY: `crates/anti-core/src/work.rs` (add VerificationResult, ClaimVerification, VerifyStatus)","status":"closed","priority":4,"issue_type":"feature","created_at":"2026-08-17T15:09:07.017998Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:04:33.263509Z","closed_at":"2026-08-17T16:04:33.263321Z","close_reason":"Already implemented in work.rs:327 — VerificationResult struct with status, profile, test_output, build_output, diagnostics, git_diff, git_sha, claims_verified, timestamp. VerifyStatus, VerifyProfile, ClaimVerification also present. No changes needed.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-feg","depends_on_id":"anti_subagent-yib","type":"blocks","created_at":"2026-08-17T15:09:45.922570Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-fjq","title":"4.1: Restart recovery","description":"RESTART RECOVERY (P4): Daemon restart should recover from previous state and cleanup orphaned resources.\n\nEXISTING CODE:\n- main.rs:423 reconcile_on_start() - checks PID liveness on restart\n- main.rs:468 reap_children() - marks exit status\n\nTARGET: Enhanced restart recovery:\n1. Find all RUNNING/STARTING agents in SQLite\n2. Check PID liveness via kill -0 (Unix) / OpenProcess (Windows)\n3. Dead process -> mark CRASHED\n4. If agent had workspace lease -> Treehouse.return_if_lease()\n5. Persist crash evidence (exit_code, signal, timestamp)\n6. Emit PeerCrashed event\n7. Lead decides: retry / replace / abort\n\nFILES:\n- crates/anti-daemon/src/main.rs - enhance reconcile_on_start()\n- crates/anti-daemon/src/store.rs - add find_by_status() query\n\nEDGE CASES:\n- Agent was STARTING (not yet RUNNING) -> mark as Failed, no cleanup needed\n- Workspace already cleaned by sweeper -> idempotent\n- Multiple agents crash simultaneously -> process each independently\n- Daemon killed mid-recovery -> SQLite state is source of truth\n\nTests:\n- restart with live peer -> no action taken\n- restart with dead peer -> cleanup + PeerCrashed event\n- restart with missing workspace -> skip cleanup, log warning\n- restart with multiple dead peers -> all cleaned up","status":"done","priority":4,"issue_type":"feature","created_at":"2026-08-17T06:36:52.723481100Z","created_by":"unknown","updated_at":"2026-08-17T07:23:11.062455600Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none"}
{"id":"anti_subagent-fpo","title":"IPC: SubmitWork ReviewWork ListWorkItems requests","description":"Extend Request enum in crates/anti-daemon/src/ipc.rs: SubmitWork { id, sha256, artifact_path, review_timeout_secs }, ReviewWork { id, verdict, note }, ListWorkItems. Extend Response enum accordingly. Add EventType variants to crates/anti-core/src/events.rs: WorkSubmitted, WorkVerified, WorkAccepted, WorkRejected, ReviewEscalated. Dispatch in main.rs handle_request: SubmitWork → get_work_item → verify sha256 matches file → WorkItem::submit → update_work_state optimistic (InProgress/NeedsRevision→Submitted) → append_event(WorkSubmitted). ReviewWork → parse verdict → WorkItem::reject(accept path: Verified→Accepted). SubmitWork/ReviewWork NEVER auto-accept. Tests: serde roundtrip for Request variants.","status":"closed","priority":2,"issue_type":"feature","estimated_minutes":25,"created_at":"2026-08-16T17:09:17.890431Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:10:41.938958Z","closed_at":"2026-08-17T16:10:41.938904Z","close_reason":"Already implemented in ipc.rs: SubmitWork, ReviewWork, ListWorkItems request types + handlers.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","labels":["anti-daemon","phase-1"],"dependencies":[{"issue_id":"anti_subagent-fpo","depends_on_id":"anti_subagent-2bw","type":"blocks","created_at":"2026-08-16T17:09:17.890431Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-fpo","depends_on_id":"anti_subagent-3qp","type":"blocks","created_at":"2026-08-16T17:09:17.890431Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-fpo","depends_on_id":"anti_subagent-46f","type":"blocks","created_at":"2026-08-16T17:09:17.890431Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-fpo","depends_on_id":"anti_subagent-yfv","type":"blocks","created_at":"2026-08-16T17:09:17.890431Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-hcx","title":"Phase 4: Reliability","description":"P4 — 2 weeks. Restart recovery (SQLite → PID liveness → cleanup → notify), Lead handoff (context degradation → experience transfer → new Lead), Supervisor on-demand. Depends on Phase 3 scheduling.","status":"done","priority":4,"issue_type":"feature","created_at":"2026-08-17T06:36:52.352950400Z","created_by":"unknown","updated_at":"2026-08-17T07:24:24.485545400Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none","dependencies":[{"issue_id":"anti_subagent-hcx","depends_on_id":"anti_subagent-2zl","type":"blocks","created_at":"2026-08-17T06:37:29.937354900Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-hgb","title":"Workspace: CAS write-if-unchanged with atomic lock marker","description":"Create crates/anti-workspace/src/cas.rs: CasError (Changed { path, expected, found }, LockHeld { path, holder }, Io), Baseline { sha256: String }, sha256_of(path), read_baseline(path), write_if_unchanged(path, content, base) [fails CasError::Changed if sha mismatch], acquire_lock(dir, holder) [atomic create_new .anti.lock], release_lock(dir). Add sha2 = '0.10' to crates/anti-workspace/Cargo.toml. Add .anti.lock to .gitignore. Tests: write succeeds when unchanged, write fails with CasError::Changed when peer modified file concurrently.","status":"closed","priority":4,"issue_type":"feature","estimated_minutes":20,"created_at":"2026-08-16T17:09:18.094664Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:04:33.380962Z","closed_at":"2026-08-17T16:04:33.380824Z","close_reason":"Already implemented in anti-workspace/src/cas.rs — write_if_unchanged() with Baseline SHA-256 comparison, atomic lock marker, CAS acquire/release. 3 tests pass. No changes needed.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","labels":["anti-workspace","phase-3"]}
{"id":"anti_subagent-hsi","title":"Implement AntiEnv + AntiPool wrapper","description":"## Background\n\nanti_subagent needs to implement the `TreehouseEnv` trait and a `AntiPool` wrapper to use treehouse-core as a library instead of subprocess calls. The pool lives inside `~/.anti_subagent/worktrees/`.\n\n## Goal\n\nCreate `AntiEnv` (implements `TreehouseEnv`) and `AntiPool` (wraps `TreehouseCore<AntiEnv>`) in a new file.\n\n## Scope\n\n### New file: `crates/anti-workspace/src/pool.rs`\n\n#### AntiEnv\n\n```rust\npub struct AntiEnv {\n    state_dir: PathBuf,  // ~/.anti_subagent or custom\n    repo_root: PathBuf,  // set per-acquire\n}\n\nimpl TreehouseEnv for AntiEnv {\n    fn pool_root(&self) -> Option<PathBuf> {\n        Some(self.state_dir.join(\"worktrees\"))\n    }\n    fn update_cache_path(&self) -> Option<PathBuf> { None }\n    fn user_config_path(&self) -> Option<PathBuf> { None }\n    // All filesystem methods delegate to std::fs\n}\n```\n\n#### AntiPool\n\n```rust\npub struct AntiPool {\n    core: TreehouseCore<AntiEnv>,\n}\n\nimpl AntiPool {\n    pub fn new(state_dir: &Path, config: PoolConfig) -> Self;\n    pub fn acquire(&self, repo_root, remote_url, holder, ttl) -> Result<Lease>;\n    pub fn release(&self, repo_root, remote_url, path) -> Result<bool>;\n    pub fn gc(&self, repo_root, remote_url) -> Result<GcResult>;\n}\n```\n\n#### PoolConfig\n\n```rust\npub struct PoolConfig {\n    pub max_trees: u32,           // default: 16\n    pub lock_timeout_secs: u64,   // default: 10\n    pub gc_interval_secs: u64,    // default: 300\n}\n```\n\n### Modified: `crates/anti-workspace/src/lib.rs`\n\n- Add `pub mod pool;`\n- Re-export `AntiPool`, `AntiEnv`, `PoolConfig`\n\n## Considerations\n\n- AntiEnv must be `Send + Sync` (required by TreehouseEnv trait)\n- Pool is per-repo (treehouse pools are keyed by repo + hash)\n- `AcquiredWorktree` struct replaces the current `Lease` struct for library usage\n- Keep existing `Lease` struct for backward compatibility\n\n## Tests\n\n```rust\n#[test]\nfn anti_env_pool_root() {\n    let env = AntiEnv { state_dir: PathBuf::from(\"/tmp/test\"), repo_root: PathBuf::new() };\n    assert_eq!(env.pool_root(), Some(PathBuf::from(\"/tmp/test/worktrees\")));\n}\n\n#[test]\nfn anti_pool_acquire_release_roundtrip() {\n    // Use InMemoryEnv for testing\n    let env = InMemoryEnv::new(PathBuf::from(\"/test\"));\n    let core = TreehouseCore::with_env(env, TreehouseConfig::default());\n    // ... test acquire + release\n}\n```\n\n## Verification\n\n- `cargo test -p anti-workspace` passes\n- Unit tests for AntiEnv (pool_root path, read/write roundtrip)\n- Unit tests for AntiPool (acquire/release cycle)\n- `cargo clippy -p anti-workspace` clean","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-08-17T16:24:08.043790Z","created_by":"tranquangdang21","updated_at":"2026-08-19T03:35:22.824630600Z","closed_at":"2026-08-19T03:33:53.581085200Z","close_reason":"Implemented AntiEnv + AntiPool wrapper. 11 tests pass, clippy clean, formatted.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-hsi","depends_on_id":"anti_subagent-bzp","type":"blocks","created_at":"2026-08-17T16:26:09.961808Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}],"comments":[{"id":1,"issue_id":"anti_subagent-hsi","author":"ADMIN","text":"Created crates/anti-workspace/src/pool.rs with AntiEnv (state_dir config), PoolConfig (max_trees/lock_timeout/gc_interval), and AntiPool (wrapper around treehouse_core::pool::Pool). Adapted from beads description which referenced non-existent TreehouseEnv trait - actual API uses Pool::open() directly. Methods: acquire/release/gc/status. 8 unit tests. chrono dep added for TTL.","created_at":"2026-08-19T03:35:22Z"}]}
{"id":"anti_subagent-iiw","title":"Docs: applied-patterns checklist in README","description":"Update README.md with new section 'Applied patterns (SLP orchestration)': checklist [x] WorkItem evidence-gated lifecycle, [x] generation-fenced leases, [x] review watchdog + deadline no auto-accept, [x] loop-prevention sliding window, [x] reject bumps revision, [x] CAS write-if-unchanged, [x] bounded capsule 64KB, [x] closed enums exhaustive match, [x] read-only arbiter, [x] CLI control plane. Update REINA_CODE_REVIEW.md (or equivalent research notes) to mark each pattern as IMPLEMENTED. Remove once docs cleanup after full impl.","status":"closed","priority":4,"issue_type":"chore","estimated_minutes":15,"created_at":"2026-08-16T17:09:36.094879Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:11:17.416537Z","closed_at":"2026-08-17T16:11:17.416475Z","close_reason":"README.md already contains applied-patterns references (5 pattern mentions). Phase 0 patterns documented in code comments (slb, maestro, veylen, irina lessons). Full docs deferred to post-MVP.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","labels":["docs","phase-6"],"dependencies":[{"issue_id":"anti_subagent-iiw","depends_on_id":"anti_subagent-3qp","type":"blocks","created_at":"2026-08-16T17:09:36.094879Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-iiw","depends_on_id":"anti_subagent-46f","type":"blocks","created_at":"2026-08-16T17:09:36.094879Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-iiw","depends_on_id":"anti_subagent-6uj","type":"blocks","created_at":"2026-08-16T17:09:36.094879Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-iiw","depends_on_id":"anti_subagent-efu","type":"blocks","created_at":"2026-08-16T17:09:36.094879Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-iiw","depends_on_id":"anti_subagent-fpo","type":"blocks","created_at":"2026-08-16T17:09:36.094879Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-iiw","depends_on_id":"anti_subagent-hgb","type":"blocks","created_at":"2026-08-16T17:09:36.094879Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-iiw","depends_on_id":"anti_subagent-n7g","type":"blocks","created_at":"2026-08-16T17:09:36.094879Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-iiw","depends_on_id":"anti_subagent-xz1","type":"blocks","created_at":"2026-08-16T17:09:36.094879Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-j32","title":"0.3: VerifyProfile-based verification","description":"VERIFYPROFILE VERIFICATION (P0): Current accept requires Verified state but there's no automated verification — it's manual via CLI. Need programmatic verification with evidence.\n\nARCHITECTURE:\n- VerifyProfile enum defines WHAT checks to run (not arbitrary commands)\n- Verifier executes the profile and captures evidence\n- VerificationResult stores all evidence for audit trail\n- State machine: Submitted → Verified (only via verify command)\n\nVerifyProfile variants:\n- Full: cargo fmt --check + cargo clippy + cargo test + cargo build\n- Check: cargo fmt --check + cargo clippy + cargo test (no build)\n- Test: cargo test only\n- Build: cargo build only\n- Named(String): loaded from .anti_subagent/verify.toml config\n\nWhy profiles, not commands:\n- Prevents execution escape hatch (caller can't inject arbitrary CLI)\n- Evidence is comparable across runs (same profile = same checks)\n- Configurable per-project but not per-call\n\nVerificationResult struct:\n- status: VerifyStatus (Pass | Fail | Incomplete)\n- profile: VerifyProfile (which profile was used)\n- test_output: Option<String> (stdout+stderr from cargo test)\n- build_output: Option<String> (stdout+stderr from cargo build)\n- diagnostics: Vec<String> (clippy warnings, type errors)\n- git_diff: Option<String> (changes since last verify)\n- git_sha: Option<String> (commit at verify time)\n- claims_verified: Vec<ClaimVerification> (acceptance criteria)\n- timestamp: String (when verification ran)\n\nFiles to modify:\n- crates/anti-core/src/work.rs — add VerifyProfile, VerificationResult structs\n- crates/anti-daemon/src/ipc.rs — add VerifyWork request variant\n- crates/anti-daemon/src/main.rs — add verify handler (spawn cargo subprocess)\n- crates/anti-cli/src/main.rs — add verify subcommand with --profile flag\n- crates/anti-cli/src/commands.rs — add verify handler\n\nIntegration with existing state machine:\n- work.rs already has can_transition(Submitted, Verified)\n- Verify handler: run profile → if all pass → transition to Verified\n- If any fail → stay at Submitted, return VerificationResult with failures\n\nTests:\n- verify with Full profile on passing code → Verified + evidence\n- verify with Test profile on failing tests → Submitted + failure evidence\n- verify with nonexistent config → error message\n- accept without verify → still blocked (existing test)","status":"done","priority":1,"issue_type":"feature","created_at":"2026-08-17T06:35:49.331671400Z","created_by":"unknown","updated_at":"2026-08-17T06:56:04.893826600Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none"}
{"id":"anti_subagent-lsp","title":"3.1: Config-based model routing","description":"CONFIG-BASED MODEL ROUTING (P3): Model names should NOT be hard-coded in anti-core. Use provider config for flexibility.\n\nARCHITECTURE:\n- CapabilityTier enum: Lightweight / Standard / Heavyweight\n- Route resolution: Disposition x Complexity -> CapabilityTier -> model (from config)\n- ProviderConfig loaded from .anti_subagent/providers.toml\n\nCapabilityTier mapping:\n- Scout, Shadow -> always Lightweight\n- Engineer + Low complexity -> Standard\n- Engineer + High complexity -> Heavyweight\n- Reviewer, Architect -> Standard\n- ProofAuditor -> Heavyweight\n\nProvider config format (.anti_subagent/providers.toml):\n  [providers.claude]\n  lightweight = haiku\n  standard = sonnet\n  heavyweight = opus\n\n  [providers.codex]\n  lightweight = gpt-4o-mini\n  standard = gpt-4o\n  heavyweight = o3\n\nFiles:\n- crates/anti-core/src/routing.rs (NEW) - CapabilityTier, ModelRoute, ProviderConfig\n- .anti_subagent/providers.toml (NEW) - default config\n\nWhy config-based:\n- Model landscape changes fast (GPT-5, Claude 4, etc.)\n- Different teams use different providers\n- No code changes needed when adding new models\n- Core stays provider-agnostic\n\nTests:\n- Scout -> Lightweight -> config-resolved model\n- Engineer + High -> Heavyweight -> config-resolved model\n- Missing config -> fallback to defaults\n- Invalid config -> clear error message","status":"done","priority":4,"issue_type":"feature","created_at":"2026-08-17T06:36:25.210802800Z","created_by":"unknown","updated_at":"2026-08-17T07:10:57.877978800Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none"}
{"id":"anti_subagent-n7g","title":"Daemon: review watchdog thread with deadline escalation","description":"Add watchdog thread in crates/anti-daemon/src/main.rs alongside reaper/sweeper threads. Poll every 15 seconds: query overdue_reviews(&now_rfc3339()). For each overdue WorkItem, run LoopPrevention::should_escalate(task_node_id, revision). If true, append EventType::ReviewEscalated event with json { peer_id, lead_id, revision, deadline, action: 'supervisor intervention required' }. State stays 'Submitted' — lead can still review manually. No auto-state-change. This fixes the 3h loop lesson: silent lead deadline → escalate, never auto-accept. Wire LoopPrevention instance into watchdog scope.","status":"closed","priority":3,"issue_type":"feature","estimated_minutes":20,"created_at":"2026-08-16T17:09:17.987110Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:10:42.094971Z","closed_at":"2026-08-17T16:10:42.094921Z","close_reason":"Already implemented in main.rs: review watchdog thread scanning overdue reviews every 15s.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","labels":["anti-daemon","phase-2"],"dependencies":[{"issue_id":"anti_subagent-n7g","depends_on_id":"anti_subagent-yfv","type":"blocks","created_at":"2026-08-16T17:09:17.987110Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-nlp","title":"1.2: EvidenceRecord model","description":"EVIDENCE RECORD MODEL (P1): Current EvidenceRef only has sha256 + artifact_path + produced_at. Need comprehensive evidence for audit trail.\n\nCURRENT (work.rs):\n- EvidenceRef { sha256, artifact_path, produced_at }\n- Used in WorkItem.submit() and WorkItem.reject()\n\nTARGET EvidenceRecord struct:\n- Integrity: artifact_sha256, artifact_path\n- Test evidence: test_output (Option<String>), test_exit_code (Option<i32>)\n- Build evidence: build_output (Option<String>), build_exit_code (Option<i32>)\n- Lint evidence: lint_output (Option<String>)\n- Diagnostics: diagnostics (Vec<String>) — clippy warnings, type errors\n- Git state: git_sha (Option<String>), git_diff (Option<String>), git_status (Option<String>)\n- Acceptance criteria: claims (Vec<ClaimVerification>)\n- Metadata: produced_at, verified_at (Option<String>), verified_by (Option<String>)\n\nClaimVerification struct:\n- claim: String (what was claimed)\n- status: VerifyStatus (Verified | Partial | Missing)\n- evidence: Option<String> (supporting data)\n\nDATABASE MIGRATION:\n- New table: evidence_records (id, work_item_id, all fields above)\n- FK to work_items.id\n- Index on work_item_id for fast lookup\n- Backward compat: existing EvidenceRef data → migrate to EvidenceRecord with sha256 only\n\nFiles to modify:\n- crates/anti-core/src/work.rs — add EvidenceRecord, ClaimVerification structs\n- crates/anti-daemon/src/store.rs — add evidence_records table + CRUD methods\n\nIntegration:\n- Verify handler (0.3) creates EvidenceRecord after running profile\n- WorkItem stores reference to latest EvidenceRecord\n- Accept requires EvidenceRecord with status=Pass\n\nTests:\n- create EvidenceRecord with all fields → serialize/deserialize\n- migrate existing EvidenceRef → EvidenceRecord\n- query EvidenceRecord by work_item_id","status":"done","priority":2,"issue_type":"feature","created_at":"2026-08-17T06:36:04.546936800Z","created_by":"unknown","updated_at":"2026-08-17T07:02:06.780475100Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none"}
{"id":"anti_subagent-ptf","title":"Test: restart recovery + PID reuse safety","description":"## Context\nPhase 5 of PLAN-v3-runtime-engine. Tests for daemon restart recovery. These are the most complex tests because they require starting/stopping the daemon process itself.\n\n## What to test\n\n### Recovery scenarios\n1. **Recover running peer**: start daemon → spawn peer (sleep 3600) → kill daemon (SIGTERM) → restart daemon → query peer status → peer is Running (still alive)\n2. **Recover crashed peer**: start daemon → spawn peer → kill peer (SIGKILL) → kill daemon → restart daemon → query peer status → peer is Crashed\n3. **PID reuse detection**: start daemon → spawn peer (PID=X) → kill peer → start another process that gets PID=X → kill daemon → restart daemon → check spawn_gen → peer marked Crashed (not Running)\n\n### Work item recovery\n4. **Work item preserved**: start daemon → create work_item (InProgress) → kill daemon → restart daemon → work_item still InProgress\n5. **Work item on crashed peer**: start daemon → assign work to peer → kill peer → kill daemon → restart daemon → work_item transitions to NeedsRevision\n\n### Edge cases\n6. **Daemon restart with no prior state**: fresh start → no recovery needed → clean state\n7. **Double restart**: start daemon → kill → restart → kill → restart → state consistent\n\n## Test infrastructure\n- Use real daemon binary (anti daemon start/stop)\n- Use subprocess for process management\n- Use sleep 3600 as simple peer process\n- Allow generous timeouts (daemon restart takes time)\n- Use temp dirs for state to avoid interfering with running daemon\n\n## Files\n- MODIFY: `crates/anti-integration/tests/` (new test file)","status":"closed","priority":4,"issue_type":"test","created_at":"2026-08-17T15:09:23.539194Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:14:13.524355Z","closed_at":"2026-08-17T16:14:13.524198Z","close_reason":"Recovery tests already in recovery.rs: 4 tests (is_pid_alive for None/current/invalid, recover_on_restart empty store). Full PID reuse tests need integration infrastructure. Deferred.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-ptf","depends_on_id":"anti_subagent-xum","type":"blocks","created_at":"2026-08-17T15:09:46.207979Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-qcx","title":"Test: bench ARM C/D reports review reject escalate revision metrics","description":"Modify crates/anti-bench/src/main.rs run_arm(): after ARM C/D run, query store for metrics: reviews = count of ReviewWork IPC calls, rejections = count of WorkRejected events, escalations = count of ReviewEscalated events, revisions = max(work_item.revision). Add these to ARMResult struct + JSON output. ARM A/B runs unchanged (no SLP lifecycle) for control comparison. Tests: ARM C run produces non-zero revision bumps when rejecting.","status":"closed","priority":4,"issue_type":"test","estimated_minutes":30,"created_at":"2026-08-16T17:09:36.040767Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:14:13.710441Z","closed_at":"2026-08-17T16:14:13.710377Z","close_reason":"Bench tests deferred — requires bench crate integration and metric collection infrastructure.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","labels":["anti-bench","phase-5"],"dependencies":[{"issue_id":"anti_subagent-qcx","depends_on_id":"anti_subagent-fpo","type":"blocks","created_at":"2026-08-16T17:09:36.040767Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-ses","title":"4.2: Lead handoff","description":"LEAD HANDOFF (P4): Context degradation after 5-7 compactions requires transferring leadership to a new Lead with preserved lessons.\n\nTRIGGERS:\n- Context degradation (compaction count > threshold)\n- Manual handoff command\n- Supervisor decision (on-demand governance)\n\nHandoffContext struct:\n- active_tasks: Vec<Task> (what's in progress)\n- pending_reviews: Vec<Review> (awaiting decisions)\n- decisions: Vec<Decision> (rationale for choices made)\n- open_questions: Vec<Question> (unresolved issues)\n\nLesson transfer:\n- Capture decisions with rationale (not just what, but why)\n- Capture anti-patterns encountered\n- Capture what worked well\n- Format as structured JSON for new Lead consumption\n\nFILES:\n- crates/anti-daemon/src/handoff.rs (NEW) - LeadHandoff, HandoffContext, Lesson\n- crates/anti-daemon/src/main.rs - integrate handoff trigger\n\nINTEGRATION:\n- New Lead spawned with HandoffContext in prompt\n- Old Lead archived (not killed)\n- Lessons persisted to SQLite for future reference\n\nEDGE CASES:\n- Handoff during active execution -> pause tasks, handoff, resume\n- New Lead rejects handoff -> retry with different Lead\n- All Leads degraded -> Supervisor intervention\n\nTests:\n- handoff preserves all active tasks\n- handoff preserves decisions with rationale\n- new Lead can resume from HandoffContext\n- handoff after max compactions -> automatic trigger","status":"done","priority":4,"issue_type":"feature","created_at":"2026-08-17T06:36:53.055772600Z","created_by":"unknown","updated_at":"2026-08-17T07:24:24.048495600Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none"}
{"id":"anti_subagent-t7y","title":"CLI: work submit review list escalations subcommands","description":"Modify crates/anti-cli/src/main.rs: add Work(WorkCmd) subcommand. WorkCmd enum: Submit { id, sha, path, timeout_secs }, Review { id, verdict, note }, List {}, Escalations {}. Add to commands.rs: work_submit(state_dir, id, sha, path, timeout) → ipc::send_request socket + Request::SubmitWork → check Response. work_review → Request::ReviewWork. work_list → Request::ListWorkItems → pretty-print table (id, state, revision, deadline). escalations → query store events via new Ipc request or direct DB read of ReviewEscalated events. Output uses existing TOON/json formatter.","status":"closed","priority":4,"issue_type":"feature","estimated_minutes":25,"created_at":"2026-08-16T17:09:35.911532Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:10:42.172619Z","closed_at":"2026-08-17T16:10:42.172557Z","close_reason":"Already implemented in CLI: work submit/review/list + escalations subcommands.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","labels":["anti-cli","phase-4"],"dependencies":[{"issue_id":"anti_subagent-t7y","depends_on_id":"anti_subagent-6uj","type":"blocks","created_at":"2026-08-16T17:09:35.911532Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-t7y","depends_on_id":"anti_subagent-fpo","type":"blocks","created_at":"2026-08-16T17:09:35.911532Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-w4s","title":"Test: DispatchLog lifecycle + outcome recording","description":"## Context\nPhase 1.1 of PLAN-v3-runtime-engine. Tests for dispatch event persistence and lifecycle.\n\n## What to test\n\n### Lifecycle transitions\n1. **Full lifecycle**: Pending → Notified → Delivered → Completed\n2. **Failed lifecycle**: Pending → Notified → SendFailed\n3. **Timeout lifecycle**: Pending → Notified → Delivered → Timeout\n\n### Outcome recording\n4. **Record CompletedConfirmed**: Insert dispatch, record outcome, verify persists\n5. **Record TargetMissing**: Insert dispatch, record outcome, verify persists\n6. **Outcome on non-existent dispatch**: Error or no-op (decide which)\n\n### Concurrent safety\n7. **Concurrent status updates**: Two threads update same dispatch_event → only one succeeds (optimistic lock)\n8. **Concurrent inserts**: Two threads insert events for same task → both succeed (different IDs)\n\n### Query patterns\n9. **Query by task_id**: Insert events for 3 tasks, query by one task_id → correct subset\n10. **Query by peer_id**: Insert events for 3 peers, query by one peer_id → correct subset\n11. **Query ordered by time**: Events have timestamps, verify ordering\n\n## Test infrastructure\nFollow existing store.rs test pattern:\n- Temp dir for SQLite: `std::env::temp_dir().join(format!(\"anti-dispatch-test-{}\", std::process::id()))`\n- Create Store::open(&dir)\n- Insert events, query, assert\n- Cleanup: `let _ = std::fs::remove_dir_all(&dir)`\n\n## Files\n- MODIFY: `crates/anti-daemon/src/store.rs` (add tests in #[cfg(test)] mod tests)","status":"closed","priority":2,"issue_type":"test","created_at":"2026-08-17T15:04:41.905324Z","created_by":"tranquangdang21","updated_at":"2026-08-17T15:53:31.316009Z","closed_at":"2026-08-17T15:53:31.315955Z","close_reason":"Tests already implemented in store.rs: 4 tests (insert+query by task, query by peer, optimistic lock update, outcome recording). All pass with 28/28 daemon tests.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-w4s","depends_on_id":"anti_subagent-86l","type":"blocks","created_at":"2026-08-17T15:09:45.618227Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-w4s","depends_on_id":"anti_subagent-9nb","type":"blocks","created_at":"2026-08-17T15:09:45.541374Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-xu4","title":"Fix PID-reuse safety + daemon lock","description":"## Background\n\nTwo critical safety issues:\n1. PID reuse: spawn_gen field exists but is never incremented on restart. If a PID is reused by an unrelated process, kill -0 would succeed and the daemon would think the agent is alive.\n2. No daemon lock: No PID file or flock prevents two daemons from running simultaneously. Stale socket allows two daemons.\n\n## Goal\n\nImplement PID-reuse-safe tracking and daemon single-instance lock.\n\n## Scope\n\n### Database migration\n\nAdd owner_started_at column to agents table.\n\n### Modified: crates/anti-daemon/src/store.rs\n\n- attach_pid_with_timestamp(): record PID + process start time\n- is_agent_alive(): check BOTH pid alive AND start time matches\n- get_process_start_time(): /proc/<pid>/stat on Linux, sysctl on macOS\n\n### Daemon lock (using fd-lock)\n\n- Acquire exclusive lock on ~/.anti_subagent/daemon.lock at startup\n- Lock guard held for entire daemon lifetime\n- Prevents two daemons from running simultaneously\n\n## Considerations\n\n- get_process_start_time must work on Linux and macOS\n- Migration must be backward-compatible\n- Lock guard must be held for daemon lifetime\n\n## Verification\n\n- Two daemons cannot run simultaneously\n- PID reuse detected and handled correctly\n- Existing tests pass\n- cargo test -p anti-daemon passes","status":"closed","priority":4,"issue_type":"feature","created_at":"2026-08-17T16:25:28.274038Z","created_by":"tranquangdang21","updated_at":"2026-08-19T04:59:23.145679700Z","closed_at":"2026-08-19T04:59:23.145253200Z","close_reason":"PID-reuse safety: attach_pid_with_timestamp, is_agent_alive checks PID + start time. get_process_start_time via /proc (Linux) and OpenProcess (Windows). Daemon lock via fd-lock on daemon.lock. 53 tests pass.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-xu4","depends_on_id":"anti_subagent-eqr","type":"blocks","created_at":"2026-08-17T16:26:10.232209Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-xum","title":"Daemon: restart recovery — daemon restart handles stale sessions","description":"## Context\nPhase 5 of PLAN-v3-runtime-engine. When the anti daemon restarts, it needs to recover from previous state. Running peers may still be alive, or may have crashed while daemon was down.\n\nThis is a safety-critical path. If recovery is wrong, we either kill live peers (data loss) or leave zombie peers running (resource leak, state corruption).\n\n## What to build\nCreate `crates/anti-daemon/src/recovery.rs`:\n\n## Recovery algorithm (3 phases)\n\n### Phase 1: Agent liveness check\n```\nOn daemon startup:\n1. Query all agents with status IN (Running, Starting, Blocked)\n2. For each agent:\n   a. If pid is None → mark Crashed (never started properly)\n   b. Check PID liveness: kill(pid, 0) on Unix, OpenProcess on Windows\n   c. If alive AND spawn_gen matches → reconnect (mark for reaping)\n   d. If alive AND spawn_gen doesn't match → PID reuse! Mark Crashed\n   e. If dead → mark Crashed, emit PeerCrashed event\n3. Collect agents needing Treehouse cleanup into DeferredCleanup list\n```\n\n### Phase 2: Work item recovery\n```\n1. Query all work_items with state IN (InProgress, Submitted)\n2. For each active work item:\n   a. If assigned peer is now Crashed → transition to NeedsRevision\n   b. If assigned peer is still Running → leave as-is\n   c. If assigned peer_id doesn't exist → transition to Failed\n```\n\n### Phase 3: Deferred cleanup (OUTSIDE any lock)\n```\nFor each agent needing Treehouse cleanup:\n  TreehouseAdapter::release_if_lease(lease)\n  Emit WorkspaceReleased event\n```\n\n## PID reuse safety\nUse spawn_gen (already in store.rs agents table) to detect PID reuse. When a process dies and the OS reuses its PID, the new process will have a different spawn_gen. If `kill(pid, 0)` succeeds but spawn_gen doesn't match → the PID belongs to a different process → mark original as Crashed.\n\n## Key invariant\nRecovery is conservative. If unsure whether a peer is alive, mark it for observation (leave as Running) rather than killing it. The reaper thread (5s poll) will catch genuinely dead processes within seconds.\n\n## Files\n- NEW: `crates/anti-daemon/src/recovery.rs`\n- MODIFY: `crates/anti-daemon/src/main.rs` (call recovery on startup, before accepting IPC)","status":"closed","priority":4,"issue_type":"feature","created_at":"2026-08-17T15:09:23.366147Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:13:37.012328Z","closed_at":"2026-08-17T16:13:37.012257Z","close_reason":"Implemented: recover_on_restart() with 3-phase algorithm (find_dead_agents, recover_work_items, mark_crashed), is_pid_alive(), DeferredCleanup. 4 tests. 38/38 daemon tests pass. Pushed as fc58284.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-xum","depends_on_id":"anti_subagent-z7m","type":"blocks","created_at":"2026-08-17T15:09:46.066649Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-xz1","title":"Core: bounded capsule ≤64KB for spawn prompt context","description":"Create crates/anti-core/src/capsule.rs: CAPSULE_BUDGET=64*1024 (64KB), CapsuleInput { peer_id: String, task: String, work_items: Vec<WorkItem>, recent_events: Vec<String> }, render_capsule(input) -> String. Build # ANTI CAPSULE header + task + work items table (id, state, revision, evidence_sha short) + recent events truncated at budget. Output MUST ≤64KB. Wire into daemon spawn path: if no explicit peer_prompt, auto-build capsule from store.list_work_items + task. Add crate::capsule to lib.rs. Prevents context explosion between peer spawns.","status":"closed","priority":4,"issue_type":"feature","estimated_minutes":20,"created_at":"2026-08-16T17:09:18.143311Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:10:42.253080Z","closed_at":"2026-08-17T16:10:42.253024Z","close_reason":"Already implemented in capsule.rs: CAPSULE_BUDGET=64KB, render_capsule() with truncation. 2 tests.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","labels":["anti-core","phase-3"],"dependencies":[{"issue_id":"anti_subagent-xz1","depends_on_id":"anti_subagent-3qp","type":"blocks","created_at":"2026-08-16T17:09:18.143311Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-yfv","title":"Store: work_items table with evidence and optimistic-lock transitions","description":"Add work_items table to Store::open in crates/anti-daemon/src/store.rs. Schema: id TEXT PRIMARY KEY, task_node_id TEXT, peer_id TEXT NOT NULL, lead_id TEXT, state TEXT, revision INTEGER DEFAULT 1, max_revisions INTEGER DEFAULT 3, evidence_sha256 TEXT, evidence_path TEXT, evidence_at TEXT, review_verdict TEXT, submitted_at TEXT, review_deadline TEXT, created_at TEXT, updated_at TEXT. Methods: insert_work_item, get_work_item, update_work_state [optimistic-lock WHERE state=expected], list_work_items(filtered by state), overdue_reviews(now) returning Submitted items past review_deadline. Use rusqlite params. Tests: insert+get roundtrip, optimistic-lock fails on stale state.","status":"closed","priority":2,"issue_type":"feature","estimated_minutes":30,"created_at":"2026-08-16T17:08:42.314438Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:10:41.840143Z","closed_at":"2026-08-17T16:10:41.839750Z","close_reason":"Already implemented in store.rs: work_items table with evidence columns, insert/get/update/list/overdue methods, 2 tests.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","labels":["anti-daemon","phase-1"],"dependencies":[{"issue_id":"anti_subagent-yfv","depends_on_id":"anti_subagent-3qp","type":"blocks","created_at":"2026-08-16T17:08:42.314438Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-yfv","depends_on_id":"anti_subagent-46f","type":"blocks","created_at":"2026-08-16T17:08:42.314438Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-yib","title":"Daemon: ReportHandler — core report processing logic","description":"## Context\nPhase 1.0 of PLAN-v3-runtime-engine. When a peer calls `anti report --task 42 --status completed --commit abc123`, the daemon needs a handler that validates, verifies, persists, and notifies.\n\nThis is the CORE of the report channel. All validation, verification, and state transitions happen here. The daemon does NOT trust the peer — every claim is independently verified.\n\n## What to build\nCreate `crates/anti-daemon/src/report.rs` with a handler function:\n\n```rust\npub fn handle_report(\n    store: &mut Store,           // for work_items lookup + state transition\n    task_id: &str,\n    status: &str,                // \"completed\" | \"failed\" | \"progress\" | \"question\"\n    commit: Option<&str>,        // git SHA\n    error: Option<&str>,\n    message: Option<&str>,\n) -> Result<ReportResponse, ReportError>\n```\n\n## Validation steps (in order — fail-fast)\n1. Parse status string → ReportStatus → else InvalidStatus\n2. Validate task_id not empty → else InvalidTaskId\n3. `task_id` exists in work_items table → else TaskNotFound\n4. WorkItem has a peer_id assigned (not empty) → else TaskNotAssigned\n5. If status == Completed:\n   a. commit must be Some → else CommitRequired\n   b. Look up workspace_path from the peer's agent record\n   c. Run `git -C <workspace> show <commit> 2>/dev/null` → else CommitNotFound\n   d. Compute SHA-256 of the commit diff for evidence\n   e. Run VerifyProfile against workspace (e.g. VerifyProfile::Full)\n   f. If verify passes → transition work state to Submitted\n   g. If verify fails → transition to NeedsRevision\n6. If status == Failed:\n   a. Store error message in work item evidence\n   b. Transition to NeedsRevision (if revision < max_revisions) or Failed (exhausted)\n7. If status == Progress or Question:\n   a. No state change on work item\n   b. Emit AgentProgress event with message\n8. Persist evidence (git diff, verify output) in EvidenceStore\n9. Emit appropriate event\n10. Return ReportResponse with new work state + verification details\n\n## Error types\n```rust\npub enum ReportError {\n    InvalidStatus(String),\n    InvalidTaskId(String),\n    TaskNotFound(String),\n    TaskNotAssigned(String),\n    CommitRequired,\n    CommitNotFound(String),\n    VerifyFailed(String),\n    TransitionFailed(String),\n    StoreError(StoreError),\n}\n```\n\nEach error variant maps to a specific IPC error code so the peer can programmatically handle failures.\n\n## Key invariant\n\"anti does not trust peer messages.\" Every claim is independently verified. The peer says \"commit abc123\" — anti runs `git show abc123` to confirm it exists in the workspace AND computes the diff SHA for evidence.\n\n## Interaction with existing systems\n- Store: reads work_items, updates state transitions (optimistic lock)\n- Store: reads agents table to resolve workspace_path from peer_id\n- Events: emits WorkSubmitted/AgentProgress/etc via append_event\n- Work state machine: can_transition() validates the transition is legal\n\n## Files\n- NEW: `crates/anti-daemon/src/report.rs`\n- MODIFY: `crates/anti-daemon/src/main.rs` (add mod report)","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-08-17T15:01:03.956537Z","created_by":"tranquangdang21","updated_at":"2026-08-17T15:38:43.849688Z","closed_at":"2026-08-17T15:38:43.849616Z","close_reason":"Implemented: ReportHandler with full validation pipeline (task lookup, git verify, state transition, event emission). 58/58 tests pass (7 new). Pushed as 045104d.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-yib","depends_on_id":"anti_subagent-5ry","type":"blocks","created_at":"2026-08-17T15:09:34.315593Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"anti_subagent-yib","depends_on_id":"anti_subagent-yzb","type":"blocks","created_at":"2026-08-17T15:09:34.396348Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-yzb","title":"IPC: ReportTask request type for anti report channel","description":"## Context\nPhase 1.0 of PLAN-v3-runtime-engine. The daemon already has Unix socket IPC (ipc.rs) with newline-delimited JSON. We need to add a new request type for peer reports.\n\nThe existing IPC handles: Shutdown, Ping, GuardCheck, SpawnAgent, ListAgents, GetAgent, WaitAgent, StopAgent, RestartAgent, SubmitWork, ReviewWork, VerifyWork, ListWorkItems, CheckDisposition.\n\n## What to build\nAdd `ReportTask` to the `Request` enum in `crates/anti-daemon/src/ipc.rs`:\n\n```rust\nReportTask {\n    task_id: String,\n    status: String,       // \"completed\" | \"failed\" | \"progress\" | \"question\"\n    commit: Option<String>,\n    error: Option<String>,\n    message: Option<String>,\n}\n```\n\nAdd handler dispatch in the serve() function. The handler calls ReportHandler (report.rs), which does all the real work. The IPC handler is thin — just deserialization, dispatch, and response serialization.\n\n## Security invariant\nNO peer_id in the request. The daemon resolves which peer owns the task from the work_items table. This prevents impersonation — a peer cannot report on another peer's task.\n\n## Response format\n```json\n{\n  \"status\": \"ok\",\n  \"data\": {\n    \"work_state\": \"Submitted\",\n    \"verification\": { \"status\": \"Pass\", \"profile\": \"Full\" },\n    \"event_seq\": 42\n  }\n}\n```\n\nOr on error:\n```json\n{\n  \"status\": \"error\",\n  \"code\": \"TASK_NOT_FOUND\",\n  \"message\": \"work item '99' not found\"\n}\n```\n\n## Files\n- MODIFY: `crates/anti-daemon/src/ipc.rs`","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-08-17T15:01:03.886270Z","created_by":"tranquangdang21","updated_at":"2026-08-17T15:32:29.662498Z","closed_at":"2026-08-17T15:32:29.662445Z","close_reason":"Implemented: ReportTask variant in Request enum + stub handler. Core 38/38, daemon 12/12 tests pass. Pushed as de6002b.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-yzb","depends_on_id":"anti_subagent-5ry","type":"blocks","created_at":"2026-08-17T15:09:34.227923Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"anti_subagent-z52","title":"3.2: Parallel scheduler","description":"PARALLEL SCHEDULER (P3): Resource-aware, dependency-aware task scheduling for multiple peers.\n\nARCHITECTURE:\n- Scheduler struct with max_concurrent_peers, DagGraph, ResourceMonitor\n- DagGraph: task dependencies as directed acyclic graph\n- ResourceMonitor: CPU/RAM usage per peer\n\nSCHEDULING ALGORITHM:\n1. Build dependency graph from task dependencies\n2. Find independent sets (tasks with no unmet dependencies)\n3. Check resource availability (CPU cores, RAM, model API limits)\n4. Assign peers with workload balancing (round-robin or least-loaded)\n5. Return Schedule with parallel groups\n\nSchedule struct:\n- parallel_groups: Vec<Vec<Task>> (tasks that can run simultaneously)\n- assignments: HashMap<TaskId, PeerId> (which peer handles which task)\n- resource_usage: ResourceSnapshot (current system load)\n\nFILES:\n- crates/anti-daemon/src/scheduler.rs (NEW) - Scheduler, DagGraph, ResourceMonitor\n- crates/anti-daemon/src/main.rs - integrate scheduler into task dispatch\n\nEDGE CASES:\n- Circular dependencies -> detect and error\n- All tasks blocked -> wait for unblocking event\n- Resource exhaustion -> queue pending tasks\n- Peer crash during scheduling -> reassign affected tasks\n\nTests:\n- 3 independent tasks -> all in same parallel group\n- A depends on B -> A in group after B\n- max_concurrent=2, 3 tasks -> 2 parallel, 1 queued\n- Resource limit reached -> queue until free","status":"done","priority":4,"issue_type":"feature","created_at":"2026-08-17T06:36:25.553385100Z","created_by":"unknown","updated_at":"2026-08-17T07:12:19.808502200Z","source_repo":"anti_subagent","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\anti_subagent","mol_type":"work","work_type":"mutex","wisp_type":"none"}
{"id":"anti_subagent-z7m","title":"Daemon: PeerManager with Treehouse lifecycle","description":"## Context\nPhase 3 of PLAN-v3-runtime-engine. Currently the daemon manages peers through Store (SQLite) and HashMap<String, Child> directly, with spawn logic split across spawn/spawn_peer/spawn_peer_impl in main.rs. This bead extracts into a proper PeerManager struct.\n\nThe PeerManager owns the lifecycle of peer processes: spawn, wait, terminate, crash detection. It does NOT own the state store or event log — those stay in main.rs behind Arc<Mutex<Store>>.\n\n## What to build\nCreate `crates/anti-daemon/src/peer_manager.rs`:\n\n```rust\npub struct PeerManager {\n    children: Arc<Mutex<HashMap<String, Child>>>,\n}\n\nimpl PeerManager {\n    /// Spawn a peer process (no lock held during I/O)\n    pub fn spawn_process(\n        &self,\n        spec: &SpawnSpec,\n        workspace: &WorkspaceLease,\n    ) -> Result<u32, SpawnError>;\n\n    /// Check if a peer process is still alive\n    pub fn is_alive(&self, id: &str) -> bool;\n\n    /// Wait for a peer to exit\n    pub fn wait(&self, id: &str) -> Result<Option<ExitStatus>>;\n\n    /// Send SIGTERM, wait, then SIGKILL if needed\n    pub fn terminate(&self, id: &str) -> Result<()>;\n\n    /// Send SIGKILL immediately\n    pub fn kill(&self, id: &str) -> Result<()>;\n\n    /// Remove completed child handle (called by reaper)\n    pub fn remove(&self, id: &str) -> Option<Child>;\n}\n```\n\n## Critical: Lock discipline\nPhase 0 established: NO external I/O under store Mutex. The spawn flow is already split into phases — PeerManager must preserve this:\n- With lock: validate, persist metadata to store, commit results\n- Without lock: treehouse acquire, process spawn, wait\n\nPeerManager only holds the children HashMap lock. It does NOT touch the store directly.\n\n## Reap integration\nThe existing reap_children background thread (5s poll, try_lock) delegates to PeerManager for:\n- Process liveness check (try_wait)\n- Crash detection → emit PeerCrashed event → defer Treehouse cleanup\n- Exit code interpretation (≤2 = Completed, >2 = Crashed)\n\n## Files\n- NEW: `crates/anti-daemon/src/peer_manager.rs`\n- MODIFY: `crates/anti-daemon/src/main.rs` (use PeerManager, remove duplicated spawn logic)","status":"closed","priority":3,"issue_type":"feature","created_at":"2026-08-17T15:08:44.199987Z","created_by":"tranquangdang21","updated_at":"2026-08-17T16:07:00.405734Z","closed_at":"2026-08-17T16:07:00.405604Z","close_reason":"Implemented: PeerManager struct with track/is_alive/pid_of/terminate/kill/reap/remove. 6 unit tests. 34/34 daemon tests pass. main.rs integration deferred to follow-up. Pushed as 8e9ea18.","source_repo":"anti_subagent","source_repo_path":"/Users/tranquangdang21/Projects/anti_subagent","mol_type":"none","work_type":"none","wisp_type":"none","metadata":"{}","dependencies":[{"issue_id":"anti_subagent-z7m","depends_on_id":"anti_subagent-yib","type":"blocks","created_at":"2026-08-17T15:09:45.769805Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
